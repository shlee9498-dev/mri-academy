# DESIGN.md — MRI ACADEMY × G드컵 디자인 계약 v2

적용 범위: mri-academy 전 페이지 + gmi-clancup 공개·방송 페이지.
이 파일이 기준점이다 — 새 세션·새 페이지는 PRODUCT.md와 이 파일을 읽고 시작한다.
(v1 "OLED Premium"을 계승·대체. 충돌 시 이 파일이 우선.)

## 0. 정체성 한 줄

**"작전 브리핑을 받는 프리미엄 e스포츠 조직"** — PUBG 공식 대회(PGC/PWS) 방송 그래픽의
정보 밀도와 규율, 군 브리핑의 단문·선언 톤, 데이터가 주인공인 화면.

## 1. 절대 금지 — AI 티 5대 패턴 (매 화면 셀프 점검 → detect 기계 재검증)

1. Inter·Roboto·시스템 기본 글꼴을 최종 글꼴로 사용
2. 보라→파랑 흐름 그라데이션 배경 (보라 계열 액센트 전반)
3. 카드 안에 카드, 그 안에 또 카드 구조
4. 색 있는 배경 위 대비 부족한 회색 본문
5. 튕기는 바운스 모션

추가 금지: 과도한 글래스모피즘(blur 배경 남용), 장식용 다색 혼용, 방패·트로피·스톡 게이밍아트,
이유 없는 무한 반복 애니메이션, 과도한 패럴랙스.

## 2. 색 — 토큰과 규칙

```css
--bg:        #0a0a0c;  /* 그라운드. 전 페이지 공통 */
--surface-1: #141419;  /* 1단 표면 — 구획은 여백·선 우선, 표면은 절제 */
--surface-2: #1b1b21;  /* 2단 표면 (테이블 행 강조 등) */
--line:      #26262c;  /* 구분선 */
--ink:       #f4f4f5;  /* 제목·핵심 텍스트 */
--ink-body:  #c6c6cd;  /* 본문 — #0a0a0c 위 대비 ≥ 4.5:1 보장값 */
--ink-dim:   #9b9ba4;  /* 보조 텍스트 — 큰 글자(18px+/bold 14px+) 전용 */
--gold:      #f5c518;  /* 유일한 액센트 */
--gold-deep: #caa00f;  /* 골드 보더·눌림 상태 */
--ok:        #4ade80;  /* 상태 보조(성공) — 아이콘·레이블 동반 필수 */
--danger:    #f87171;  /* 상태 보조(위험/LIVE 도트) — 아이콘·레이블 동반 필수 */
```

- **골드 희소 원칙**: 골드는 ①시상·순위(1위·수상) ②핵심 행동(페이지당 주 CTA 1개)
  ③핵심 수치(그 화면의 단 하나의 숫자)에만. 골드 글로우는 한 화면 1개.
- **대비 규칙(detect 검증 대상)**: 본문은 `--ink-body` 이상. `--ink-dim`은 18px+ 또는 14px bold+
  큰 글자에만. **기존 #71717a는 본문 사용 금지**(4.1:1 — 현 사이트 detect 실측 위반).
- 상태를 색에만 의존해 구분하지 않는다 — 도트·레이블·아이콘 동반.
- 보라·파랑 액센트 금지. 티어 엠블럼 원색은 "인용된 콘텐츠"로만 허용(UI 색으로 승격 금지).

## 3. 타이포그래피

- **한글 헤딩·본문: Pretendard Variable** (현행 검증됨 — 유지).
  헤딩 800, 본문 400~500. 본문 모바일 최소 16px, 행간 1.6+.
- **디스플레이(영문 레이블·숫자): Saira Condensed** (SemiBold·Bold) — 제안, 오너 승인 대상.
  콘덴스드 산세리프 = 방송 스코어보드·HUD의 글자. 대문자 + 자간 +0.08em로 레이블 사용.
  대안 후보: Barlow Condensed. (Space Grotesk·Archivo Black은 v2에서 퇴역 —
  AI 훈련데이터 기본값 목록에 올라 있는 얼굴이라 "AI 티" 회피 원칙과 충돌.)
- **숫자 규율**: 데이터 수치는 `font-variant-numeric: tabular-nums` 필수(카운터·순위표 떨림 방지).
  단위(RP·딜·판)는 수치의 60% 크기 보조 톤.
- 로드: 구글폰츠(display=swap) + Pretendard CDN 현행 유지. 시스템 폴백은 로드 실패 시에만.

## 4. 레이아웃

- 구획은 **여백·선(1px --line)·타이포 스케일**로 만든다. 박스로 감싸는 건 마지막 수단,
  카드 안에 카드 금지(1겹까지).
- 강한 정보 계층: 섹션 헤더 = [골드 눈금 마크 + 대문자 콘덴스드 레이블 + 한글 헤딩] 패턴 통일.
- 그리드: 데스크톱 12col/1200px, 모바일은 축소판 금지 — 콘텐츠 우선순위 재배열
  (증거 먼저, 장식 제거).
- 히어로는 "표지"가 아니라 "상황판": 첫 뷰포트에 실데이터(카운터·사례·일정) 최소 1개 노출.
- 스크롤 리듬: 밀도 높은 데이터 섹션 다음엔 조용한 섹션. 페이지 끝은 실질적 마무리(연락·신청 동선).

## 5. 모션

- 기본: 150~250ms ease-out 전환(호버·포커스·탭 전환). 등장은 fade-up 12px 이내 스태거.
- 바운스·엘라스틱 금지. 이유 없는 무한 반복 금지. 패럴랙스 금지.
- `prefers-reduced-motion: reduce` → 전환·등장 애니메이션 전부 무효화(즉시 표시).
- **예외 — results.html(방송)**: 점수·순위 변동은 시청자가 인지할 수 있게 명확한 트랜지션
  (행 재정렬 400ms·변동 하이라이트 플래시 1회). 방송 화면은 reduced-motion 미적용 대상.

## 6. 사운드 (청각화 — opt-in 전용)

- **자동재생 절대 금지.** 일반 페이지 사운드 금지(이탈 요인).
- results.html에만 사운드 토글: 순위 변동·라운드 갱신 짧은 전술 SFX.
  기본 음소거, 토글 상태 localStorage 유지, 파일 용량 최소(합계 100KB 이하 목표).

## 7. 접근성 (detect 60룰 + 수동 점검)

- 본문 대비 4.5:1+, 큰 글자 3:1+ (2절 토큰 준수 시 자동 충족).
- 키보드 포커스: `:focus-visible`에 골드 1px 아웃라인 + 오프셋 2px.
- 클릭·탭 영역 44×44px+. 링크는 색+밑줄/무게로 이중 구분.
- 이미지 alt, 제목 계층(h1→h2→h3) 준수, 시맨틱 태그.

## 8. 카피 규칙 (디자인과 동급)

- 단문. 선언. 데이터가 말하게. 명사형 종결·간결한 명령형(전술 브리핑 톤).
- 한국어 우선. 영문은 레이블·수치 단위만.
- 금지: 가정법 후킹("~달라질 수 있습니다"), 조급증("지금 바로"·"놓치지 마세요"),
  이모지 떡칠·느낌표 연발·물음표 후킹, 근거 없는 최상급("최고의"·"완벽한").
- 숫자 표기: 검증 탄탄한 수치는 정확히, 역사 누적은 보수 표기("30명+"·"100명 전후").
- 정직 카피 보존: "평균 6개월에서 1년이 걸립니다. 절반은 수강생의 노력입니다."

## 9. 페이지 제작 표준 루프 (매 페이지)

1. PRODUCT.md + 이 파일 읽고 시작
2. Taste Skill로 방향 → 구현 (1절 5대 패턴 셀프 점검)
3. `npx --yes impeccable@latest detect <파일>` (이 repo는 루트 평면 HTML — `detect .` 또는 개별 파일)
4. 걸린 항목 전부 수정 → 재실행 → **무출력 = 통과** (60룰 기계 검증)
5. 모바일 + 데스크톱 확인 (results는 1080p 방송 캡처 추가)
6. draft PR (before/after 스크린샷 포함)

## 10. 보호 대상 (어느 페이즈에서도 무손상)

전 API 엔드포인트·파라미터 / G드컵 시즌 처리·보너스 로직·폼 검증 / URL 경로 전부 /
SEO 메타·OG(개선 가능·삭제 금지) / footer 사업자 정보 / terms·privacy 법적 문구 /
챗봇 위젯 기능 / .env·시크릿(읽기·출력 금지) / apply.html 신청·결제 동선과 폼 필드.
