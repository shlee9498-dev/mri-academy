# MRI ACADEMY Codex 작업 규칙

## 작업 브랜치
- main 브랜치에서는 절대 파일을 수정하지 않는다.
- 작업 시작 전 `git branch --show-current`와 `git status`를 확인한다.
- main 브랜치인 경우 즉시 작업을 중단하고 사용자에게 알린다.

## 승인 규칙
- 파일 수정 전에 작업 계획과 대상 파일을 먼저 설명한다.
- 수정 후에는 `git diff`를 보여주고 한국어로 변경 내용을 설명한다.
- 사용자 승인 없이 commit, push, merge, 배포하지 않는다.
- 사용자 승인 없이 파일이나 데이터를 삭제하지 않는다.

## 보안
- API 키, 비밀번호, 토큰, 개인정보를 HTML·JS·Git에 넣지 않는다.
- `.env`와 Railway 환경변수의 실제 값은 읽거나 출력하지 않는다.
- Supabase 데이터 삭제·대량 변경은 사용자 승인 없이 실행하지 않는다.
- Discord 대량 발송과 고객 대상 메시지 발송은 사용자 승인 없이 실행하지 않는다.

## 결제 및 운영
- 가격, 환불, 사업자 정보, Toss 결제 관련 내용은 사용자 승인 없이 수정하지 않는다.
- Railway 환경변수와 실제 서비스 배포 설정은 사용자 승인 없이 변경하지 않는다.

## 브랜드
- 기본 배경은 #0a0a0c, 강조색은 #f5c518을 사용한다.
- 브랜드 관련 페이지에서 라이트 테마를 임의로 추가하지 않는다.
- canonical 및 og:url은 https://mriacademy.gg를 유지한다.

## 작업 완료 보고
- 변경된 파일 목록을 보여준다.
- 실행한 검사와 결과를 설명한다.
- 남은 위험 요소를 설명한다.
- 승인 전에는 GitHub에 반영하지 않는다.