# 트랙별 현황 (세션 간 공유)

> 세션 시작 시 이 문서를 먼저 읽는다. 작업 종료 시 **자기 트랙 섹션만** 갱신한다.
> 형식은 3줄(진행/대기/다음) 고정. 길게 쓰지 않는다 — 상세는 PR 본문과 `docs/`에 남긴다.
> 날짜는 KST. 제목의 날짜는 그 섹션을 마지막으로 갱신한 날이다.

---

## 결제 — 2026-08-05

- **진행**: 강의 결제를 `payments`에 편입할지 검토 중(편입 시 빵다 순매출 6%와 트레이너 상담 가산이 자동 증가 — Level 0). 레슨 가격 조정 8/10 적용 대기(10판 45,000 / 21판 90,000 / 33판 140,000, 반영 위치 7곳 확인 완료·미착수).
- **대기**: 오너 — ① `T2_CRON` / `DIRECT_STATUS` env 상태 회신(꺼져 있으면 크론 전체 미작동) ② 강의 결제 `payments` 편입 여부 ③ 가격 PR 준비 시점(지금 / 8-9).
- **다음**: env 확인 → 편입 여부 확정(수강관리 P1의 선행 조건) → 8/10 가격 PR 머지.

## 학습지 — 2026-08-05

- **진행**: 이 세션에서 파악된 내용 없음.
- **대기**: —
- **다음**: 해당 트랙이 첫 갱신 시 채울 것.

## MRIacademy(수강관리) — 2026-08-05

- **진행**: 오너 판정 회신 접수·시트 반영 목록 전달(중복 6건·박성민 정정·이월메모 갱신, 현재값 가드 포함). 가불 금지를 설계문서 §4.5로 문서화(P3 "재결제 안내" 요구사항 포함). **⚠ 실측 정정: §16(student_aliases)·§17(courses 3종)·payments.course_id DDL이 실제로는 미실행** — 8/4 배포 부팅 자기점검 경고 5건 + DB 전수조회로 확인(선행 "DDL 실행 완료" 기록은 착오). 따라서 import 2종·정정 SQL 전부 미적용 상태.
- **대기**: 오너 — ① §16+§17 DDL 실행 + `NOTIFY pgrst`(선행 필수) ② import 전달본 재실행 or 재생성 지시(오늘 판정 통합 재생성 권고) ③ 조현준 중복 판정 ④ 차감 누락 14건 세션별 확인 ⑤ 박성민 30,000 입금(기록은 결제 트랙) ⑥ 가불 금지 시행 시점·기존 초과자 예외.
- **다음**: DDL 실행 확인 → import+오늘 판정 통합 패키지 → 검산(김준성 17회·453,344 / 박성민 13회·초과1) → `courses.verified_at` → 본인 연결 승인 큐 → `my.html` + `/api/me` → P2 봇 커맨드.

## 메인(총괄·조정) — 2026-08-05

- **진행**: 이 문서 신설(#115) + `CLAUDE.md` 트랙 선언문을 **소관 정의표**로 재구성(#116) + 학습지 선언 블록 흡수(#117). gmi-clancup에도 갱신 규칙·제목 정정(#45). 이제 `CLAUDE.md`에 "이 세션의 소관" 문구 0건 — 소관은 정의표 한 곳에만 있다. G드컵 시즌3 종료(#29·#31·#32·#33·#35·#102·#106). **⚠ 자기 정정: 8/4 "오너 DDL 실행 완료" 기록은 오너 회신을 검증 없이 옮긴 것이었고 실제로는 미실행 — MRIacademy 트랙 실측으로 정정됨.**
- **대기**: 오너 — ① 결제·학습지·카지노 트랙 `CLAUDE.md`에 갱신 규칙 반영(해당 저장소 미접근 — 저장소명 주시면 `add_repo`로 처리) ② 각 트랙 세션에 "`CLAUDE.md`에 자기 소관 블록을 추가하지 말 것, 정의표 수정은 메인에 요청" 공지 ③ 옵저버 CSV 실기 테스트 ④ `match-pull` 리허설(응답 `season: 3`).
- **다음**: 트랙 간 충돌·공용 스키마 조정. 소관 불명 작업의 1차 분류. **오너 회신의 빈 칸을 완료로 옮기지 않는다** — 값이 없으면 대기로 남긴다.

## 카지노 — 2026-08-05

- **진행**: 이 세션에서 파악된 내용 없음. 저장소는 `gmi-casino-bot`(Python/discord.py + FastAPI, Railway).
- **대기**: —
- **다음**: 해당 트랙이 첫 갱신 시 채울 것. 코인 경제 상한은 env와 코드 기본값을 **양쪽 다** 안전값으로 유지할 것(env 단일 방어선 금지).
