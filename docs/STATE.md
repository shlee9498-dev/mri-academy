# 트랙별 현황 (세션 간 공유)

> 세션 시작 시 이 문서를 먼저 읽는다. 작업 종료 시 **자기 트랙 섹션만** 갱신한다.
> 형식은 3줄(진행/대기/다음) 고정. 길게 쓰지 않는다 — 상세는 PR 본문과 `docs/`에 남긴다.
> 날짜는 KST. 제목의 날짜는 그 섹션을 마지막으로 갱신한 날이다.
>
> **오너 조치 반영 (2026-08-05 추가).** DDL 실행·env 설정·시트 수정·입금처럼 **오너가 세션 밖에서
> 하는 일**은 어느 트랙의 "작업 종료"도 아니라서 갱신 트리거가 걸리지 않는다. 그 사실을 들은 세션이
> **타 트랙 섹션이라도 그 대기 항목을 해소 처리한다** — 이 경우만 "자기 섹션만" 규칙의 예외다.
> 대신 **무엇을 근거로 고쳤는지**(확인 쿼리·로그 등)를 한 줄로 함께 남긴다.
> 근거 없이 오너 말만으로 "완료"로 바꾸지 않는다.

---

## 결제 — 2026-08-05

- **진행**: 강의 결제를 `payments`에 편입할지 검토 중(편입 시 빵다 순매출 6%와 트레이너 상담 가산이 자동 증가 — Level 0). 레슨 가격 조정 8/10 적용 대기(10판 45,000 / 21판 90,000 / 33판 140,000, 반영 위치 7곳 확인 완료·미착수).
- **대기**: 오너 — ① `T2_CRON` / `DIRECT_STATUS` env 상태 회신(꺼져 있으면 크론 전체 미작동) ② 강의 결제 `payments` 편입 여부 ③ 가격 PR 준비 시점(지금 / 8-9).
- **다음**: env 확인 → 편입 여부 확정(수강관리 P1의 선행 조건) → 8/10 가격 PR 머지.

## 학습지 — 2026-08-05

- **진행**: 이 세션에서 파악된 내용 없음.
- **대기**: —
- **다음**: 해당 트랙이 첫 갱신 시 채울 것.

## MRIacademy(수강관리) — 2026-08-05

- **진행**: 오너 판정 회신 접수·시트 반영 목록 전달(중복 6건·박성민 정정·이월메모 갱신, 현재값 가드 포함). 가불 금지를 설계문서 §4.5로 문서화(P3 "재결제 안내" 요구사항 포함). **✅ §16(student_aliases)·§17(courses 3종)·payments.course_id DDL 실행 완료** — 확인 쿼리 4행 정상(course_attendance·course_sessions·courses·student_aliases). 8/4~8/5의 "미실행" 기록은 해소됐다. **import 2종·정정 SQL은 아직 미적용.**
- **대기**: 오너 — ① import 실행(전달본 재실행 or 오늘 판정 통합 재생성 — 재생성 권고) ② 조현준 중복 판정 ③ 차감 누락 14건 세션별 확인 ④ 박성민 30,000 입금(기록은 결제 트랙) ⑤ 가불 금지 시행 시점·기존 초과자 예외.
- **다음**: import 실행 → 검산(김준성 17회·453,344 / 박성민 13회·초과1) → `courses.verified_at` → 본인 연결 승인 큐 → `my.html` + `/api/me` → P2 봇 커맨드.
- *(이 섹션은 2026-08-05 오너 지시로 메인 트랙이 대신 갱신했다 — DDL 실행 사실이 이 트랙 세션에 전달되지 않아 어긋나 있었다. 근거: 오너 확인 쿼리 4행.)*

## 메인(총괄·조정) — 2026-08-05

- **진행**: 이 문서 신설(#115) + `CLAUDE.md` 트랙 선언문을 **소관 정의표**로 재구성(#116) + 학습지 선언 블록 흡수(#117). gmi-clancup에도 갱신 규칙·제목 정정(#45). 이제 `CLAUDE.md`에 "이 세션의 소관" 문구 0건 — 소관은 정의표 한 곳에만 있다. 스킬 정리(#121 디자인 7개 삭제·한국어 트리거 / #127 복구 절차 문서 — 독점 라이선스라 본체 이관 불가). G드컵 시즌3 종료(#29·#31·#32·#33·#35·#102·#106). **DDL 기록이 8/4 "완료"(오기록) → 8/5 "미실행"(실측) → 8/5 "완료"(실제) 로 세 번 뒤집혔다. 원인은 규칙 공백 — 오너가 세션 밖에서 한 조치를 문서에 넣는 경로가 없었다. 상단 "오너 조치 반영"으로 보강.**
- **대기**: 오너 — ① 결제·학습지·카지노 트랙 `CLAUDE.md`에 갱신 규칙 반영(해당 저장소 미접근 — 저장소명 주시면 `add_repo`로 처리) ② 각 트랙 세션에 "`CLAUDE.md`에 자기 소관 블록을 추가하지 말 것, 정의표 수정은 메인에 요청" 공지 ③ 옵저버 CSV 실기 테스트 ④ `match-pull` 리허설(응답 `season: 3`) ⑤ 스킬 트리거 실측(파일 첨부 필요).
- **다음**: 트랙 간 충돌·공용 스키마 조정. 소관 불명 작업의 1차 분류. **오너 회신의 빈 칸을 완료로 옮기지 않는다** — 값이 없으면 대기로 남긴다.

## 카지노 — 2026-08-07

- **진행**: **본선 당일. 시즌3 참가 12팀 확정 — 오너 승인 하에 Supabase 직접 반영**(백업 `gdcup_*_bak_20260807` 7테이블, RLS on·anon 권한 회수. 검산 12/12 일치, 백업 대조로 범위 밖 변경 0건). 신규 5팀(A·B·C·D조·록초리단) 등록, 티어 일괄 조정(서바이버·평딜500+ → S), **전 팀 weight를 B안 커브로 통일**(저장 weight는 자동 갱신이 안 돼 구표·신표가 섞이던 것). 가중치 B안 #133 + S 드롭다운 #52 배포. **본선 직전 블로커 2건 발견·해소** — #134 `gdcup-edit`만 상한 422가 남아 운영진 티어 조정이 원천 차단(7팀 중 4팀 저장 불가) / gmi-clancup#53 admin 저장이 `verify:true` 고정이라 오너 판정이 저장 즉시 서버 판정으로 덮어써지던 것(`🔒 판정 고정 저장` 추가). 이전 6건 머지·배포: #108 match-pull 무캐시(*이전 라운드 점수가 저장되던* 경로 차단) / gmi-clancup#42 overlay 라운드표가 시즌2 구성이던 것 + gdcup-score 401 무성실패 + 방송화면 2건 / #43 429 처리 / **#47 자동감지 429 무음 해소** / **#48 점수 입력을 확정팀으로 한정**(서버·DB 무변경, `status` 응답 활용 — 데이터 쪽 일괄 반려보다 되돌리기·영향범위에서 안전) / #46·#129 문서. **#125 확인 완료: `GDCUP_APPLY_DEADLINE` env 미설정 → 코드 기본값 8/7 20:00 KST가 유효, 의도값과 일치(근거: Railway production 변수 전수 조회)**. 잔여 3건은 `track:casino`+`본선 후` 라벨로 격리(#120·#122·#124). #122 리미터는 **도입하지 않는 것이 맞다**고 판단·오너 접수(429 출처가 PUBG 상류라 우리 리미터는 역효과).
- **대기**: 오너 — ⓪ **12팀 검산표 대조**(admin 화면과) + **딱퍼·복귀유저 `applied` → 확정 여부**(gdcup-score가 확정팀만 보여줘 이대로면 점수판에 10팀만 뜬다) + C조 신종근 IGN + B조 승희 평딜 실측 + team-brand 7팀 미등록(태그·색) + 예비인원 DDL ① **옵저버 CSV 실기 테스트**(3종 인코딩 중 어느 것을 먹는지. ⚠ 태그 미등록이라 전원 한글 2글자 폴백이고 ascii 변형도 한글을 뱉음 → 팀명 영문 표기 운영 대응이 실제로 통하는지 여기서 확인) ② **match-pull 리허설**(`season: 3` + #108 무캐시 후 첫 실기라 최신 매치가 잡히는지) ③ 로스터 교체 DB 반영(Level 0) + `contact`는 콘솔 직접(#120) ④ 운영 통제 3건 — 자동감지 1명·본선 중 팀확정 금지·운영진 gdcup-score/admin 탭 재접속(키 교체분).
- **다음**: **본선 8/7 20:00 KST 지원** → **G드컵 백엔드를 GmI 생태계로 이전하는 설계 조사(#130, 오너 방침·비수기·시즌4 개막일 역산)** → 카지노 8/9 오픈(로비는 #36 크래시 스크린샷 오너 확인이 선행). #120·#122·#124는 곧 옮겨질 코드라 **이전 후 처리가 나을 수 있다** — #130 1단계에서 함께 판단. 코인 경제 상한은 env와 코드 기본값을 **양쪽 다** 안전값으로 유지(env 단일 방어선 금지).
