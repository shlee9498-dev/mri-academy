# 트랙별 현황 (세션 간 공유)

> 세션 시작 시 이 문서를 먼저 읽는다. 작업 종료 시 **자기 트랙 섹션만** 갱신한다.
> 형식은 3줄(진행/대기/다음) 고정. 길게 쓰지 않는다 — 상세는 PR 본문과 `docs/`에 남긴다.
> 날짜는 KST. 제목의 날짜는 그 섹션을 마지막으로 갱신한 날이다.

---

## 결제 — 2026-08-05

- **진행**: 강의 결제를 `payments`에 편입할지 검토 중(편입 시 빵다 순매출 6%와 트레이너 상담 가산이 자동 증가 — Level 0). 레슨 가격 조정 8/10 적용 대기(10판 45,000 / 21판 90,000 / 33판 140,000, 반영 위치 7곳 확인 완료·미착수).
- **대기**: 오너 — ① `T2_CRON` / `DIRECT_STATUS` env 상태 회신(꺼져 있으면 크론 전체 미작동) ② 강의 결제 `payments` 편입 여부 ③ 가격 PR 준비 시점(지금 / 8-9).
- **다음**: env 확인 → 편입 여부 확정(수강관리 P1의 선행 조건) → 8/10 가격 PR 머지.

## 학습지 — 2026-08-05

- **진행**: 이 세션에서 파악된 내용 없음.
- **대기**: —
- **다음**: 해당 트랙이 첫 갱신 시 채울 것.

## MRIacademy(수강관리) — 2026-08-05

- **진행**: P0 정체 알림 머지(#111). 회차제 스키마 §17 머지(#112) + 오너 DDL 실행 완료. import SQL 2종 전달(`photo_recount` 47세션·78참가 / `sheet_import` 441행). 대조표 산출 — 차감 누락 14건(신종근 7·허혜민 2·박성민 2·길영패 1·양정현 1·양현석 1), 6/23~6/28 엿새 통째 누락, 중복 7건. **오너 확정: 김준성 17회·잔액 453,344원(답변 발송 완료) · 박성민 13회·초과 1회 정정 승인(추가입금 예정) · 중복 6건 삭제 승인(조현준 04-18은 금액 상이 42,000/21,000 → 보류) · 가불 금지 정책 신설(잔여 0 이하 수업 불가).**
- **대기**: 오너 — ① 미매칭 이름 처리(별칭 vs 신규 등록) ② 조현준 중복 판정 ③ 차감 누락 14건 시트/DB 반영 승인 ④ 가불 금지 정책의 시행 시점·기존 초과자(박성민) 예외 여부.
- **다음**: 확정분 반영 → `courses.verified_at` 기록 → 본인 연결 승인 큐(봇 DM) → `my.html` + `/api/me`(조회 전용) → P2 봇 커맨드(`/직강일정`·`/직강완료`·`/직강현황`).

## 메인(총괄·조정) — 2026-08-05

- **진행**: 세션 소관 정의를 작업 폴더 `CLAUDE.md`에 추가. 이 문서 신설 + 갱신 규칙을 mri-academy·gmi-clancup `CLAUDE.md`에 반영. 두 저장소의 소관 절 제목을 "이 세션의 소관" → "이 저장소의 주 트랙"으로 정정(저장소 파일은 그 저장소에 들어오는 모든 세션이 읽으므로, 총괄 트랙 세션이 자기를 해당 트랙으로 오인하던 문제). G드컵 시즌3 작업 종료(#29·#31·#32·#33·#35·#102·#106).
- **대기**: 오너 — ① 결제·학습지·카지노 트랙 `CLAUDE.md`에 갱신 규칙 반영(해당 저장소 미접근 — 붙여넣을 블록 전달 필요) ② 옵저버 CSV 실기 테스트 결과 ③ `match-pull` 리허설(응답 `season: 3` 확인).
- **다음**: 트랙 간 충돌·공용 스키마 조정. 소관 불명 작업의 1차 분류.

## 카지노 — 2026-08-05

- **진행**: 이 세션에서 파악된 내용 없음. 저장소는 `gmi-casino-bot`(Python/discord.py + FastAPI, Railway).
- **대기**: —
- **다음**: 해당 트랙이 첫 갱신 시 채울 것. 코인 경제 상한은 env와 코드 기본값을 **양쪽 다** 안전값으로 유지할 것(env 단일 방어선 금지).
