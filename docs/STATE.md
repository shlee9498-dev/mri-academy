# 트랙별 현황 (세션 간 공유)

> 세션 시작 시 이 문서를 먼저 읽는다. 작업 종료 시 **자기 트랙 섹션만** 갱신한다.
> 형식은 3줄(진행/대기/다음) 고정. 길게 쓰지 않는다 — 상세는 PR 본문과 `docs/`에 남긴다.
> 날짜는 KST. 제목의 날짜는 그 섹션을 마지막으로 갱신한 날이다.
>
> **오너 조치 반영 (2026-08-05 추가).** DDL 실행·env 설정·시트 수정·입금처럼 **오너가 세션 밖에서
> 하는 일**은 어느 트랙의 "작업 종료"도 아니라서 갱신 트리거가 걸리지 않는다. 그 사실을 들은 세션이
> **타 트랙 섹션이라도 그 대기 항목을 해소 처리한다** — 이 경우만 "자기 섹션만" 규칙의 예외다.
> 대신 **무엇을 근거로 고쳤는지**(확인 쿼리·로그 등)를 한 줄로 함께 남긴다.
> 근거 없이 오너 말만으로 "완료"로 바꾸지 않는다.

---

## 결제 — 2026-08-14

- **동결 설계 DDL 전량 실행 완료**(오너, 8/14). 실측 확인분 — `payments.settled_period`(nullable) · `period_locks` · `payments_history` · `payments` 트리거 2종(`trg_payments_history`·`trg_payments_lock_guard`) · **2026-07 잠금 활성**(`released_at` null) · `payouts` 10행(소급 8건 gross합 **3,876,900** · 중복 2행 정리 완료) · `staff` id=3 `comp_note` 6% 정정 · `payments` id=126 `settled_period='2026-08'` · `payment_requests.pay_channel`(nullable). 근거: `information_schema`·`pg_trigger`·`period_locks`·`payouts` 직접 조회(8/14). 이로써 **7월 정산월은 잠겼고 7월 행 INSERT/UPDATE/DELETE는 55006으로 거부**된다 — 정정은 `settled_period`를 열린 달로 지정해야 통과한다.

- **진행**: **Groble 실효율 확정 4.84%** — 오너 실결제 테스트 실측(15,000 → 정산예정 14,274 → 수수료 726). 요금표 1.5%는 플랫폼분만이었고 PG 2.9%가 별도로 붙는다(1.5+2.9=4.4 × VAT 1.1 = 4.84). `config/fees.cjs` 상수 하나만 교체. 기존 126행이 전부 `pay_channel='transfer'`라 **소급 대상 0건**. **부팅 스모크 도입**(#189 — `node --check`가 못 잡는 실행 시점 오류를 env 0개 기동으로 잡는다. #160 TDZ 형태 주입·마운트 제거 두 회귀를 실제로 잡는 것까지 확인). `package-lock.json` 신설로 `npm ci`·마운트 검증이 처음 가능해짐. 앞서 빵다 6% 대상 `kind='lesson'` 한정(`LESSON_ONLY_START="2026-08"`)과 `payBase()` 순액 전환은 #185.
- **대기**: 오너 — ① **판정 1건**(시트 버전기록 8/2 대시보드 ② 값 — 미설명 12,200. 장익교·김대윤 2건은 8/14 해소 — 하단 이탤릭) ② Gemini 정정 지시문 전달(3건 교체본 확정) ③ `staff` id=1 `active=false` UPDATE(코드는 #202로 반영됨) ⑤ **`BOT_PAYREQ=1` env**(채널 옵션 실동작 전제) ⑥ 봇 재기동(슬래시 명령 옵션 변경 반영). (④·⑦은 해소 — 하단 이탤릭)
- **다음**: 9/3 컷오버 문서(산식 정본 + `student_id` FK 완화 설계) → `payment_requests` 승인분의 `payments` 본표 편입 설계(현재는 원장 복붙 수동).
- *(**잠금 정정 변환 가이드 작성** — `docs/period-lock-correction-guide.md`. 타 트랙이 잠긴 달의 `payments`를 고쳐야 할 때 쓴다. 오류를 **A 금액**(정정 행 + `settled_period` 이월) / **B 귀속**(`student_id` 등 — **이월로 해결 안 됨**) / **C payments 밖**(잠금 무관)으로 분류한다. ⚠️ **B는 이월 경로가 없다** — −X/+X 쌍을 다음 달에 넣어도 8월에서 상쇄될 뿐 7월의 잘못된 귀속은 남는다. 빵다 6%는 행 주인을 안 보므로 영향 없으나 트레이너 정산은 영향 받는다 → 오너 판단 사안. ⚠️ **누락 보완에 이월을 쓰면 이중 반영** — 7월에 있었어야 할 행을 8월로 이월하면 없던 매출이 8월에 생긴다(정정 ≠ 누락). `games`가 `payments`·`lesson_sessions` 양쪽에 있어 테이블 확인 절차도 포함.)*
- *(**7월 차액 30,700 규명 — 3갈래로 완결, 12,200만 잔여.** `131,100`(패널/DB 28건) →**+3,200** 데이터 차이→ `134,300` →**+4,900** 시트 내부 오류→ `139,200`(시트 계산) →**+22,600** 미설명→ `161,800`(8/2 실송금). ① 데이터: 시트 42건↔DB 28건 전수 대조 — 시트에만 17건(상담 12·장익교 1·환불상쇄 세트 2쌍) / DB에만 3건(김현성·김예지·정희준 — 레슨로그엔 있고 원장 누락) / 금액차 60,000 일치. **"27건 누락"의 실체는 17건이고 71%가 상담**이며, `payments.student_id` NOT NULL 때문에 미전환 상담자는 DB에 기록 자체가 불가능하다. ② 시트 내부: 환불 3건(김준길·이지운·백진환)의 빵다수수료 열이 공란이라 미차감 +7,100, 행별내림 −2,200, 합성 +4,900 — 오너 실측으로 종결. ③ 합집합(45건)으로 재계산해도 149,600이라 **12,200은 여전히 미설명** → 시트 버전기록 대기.)*
- *(**확정 — 빵다 수수료 산식 정본**(9/3 컷오버 적용). `round100(Σ순매출 × 0.06)` 합산 후 1회 반올림. 행별 계산 후 합산 금지. 순매출은 **양수·음수 모두 반올림(round), 내림(floor) 금지**. 근거: DB 125행 실측에서 행별내림 533,900 vs 합산반올림 541,100 — 행별내림이 예외 없이 작고 손실이 행수에 비례(행당 평균 58원). 음수에서는 방향이 뒤집혀 비대칭(원결제 62원 덜 주고 환불 38원 더 깎음, 상쇄해도 0으로 안 돌아옴). 사례: 백진환 −10,000 순매출이 DB −9,100 / 시트 −9,000.)*
- *(**동결 설계 승인**(오너 8/13). 요구 4개 — ① 월 잠금(`period_locks`, 해제도 `released_at`으로 기록) ② 변경 이력(`payments_history` 트리거 — UPDATE가 흔적을 안 남기는 게 이번 조사 최대 장벽이었다) ③ `payouts` 소급 8건(`rate_snapshot`에 산출 근거 필수) ④ 정정 행 규칙 5개조. 핵심은 **`settled_period` 분리** — 마감월 INSERT를 막으면 정정 행 추가가 불가능해지므로, `paid_at`(사실)과 정산 귀속월을 분리해 정정분을 다음 열린 달로 이월한다. `id=126`(7/29 −10,000, 8/13 추가)이 첫 사례이며 `settled_period='2026-08'` 지정 시 **7월 131,100→131,600(+500) / 8월 25,600→25,000(−600)**. RLS·`payouts` 스키마 사전 확인 완료(`service_role`·`postgres` 둘 다 `rolbypassrls=true` · `payouts` ALTER 불필요 — `rate_snapshot jsonb` 기존재). **엔진 반영은 #201 머지 완료**(`settlePeriod()` + `SCHEMA_OPTIONAL` 등재. 컬럼 부재 시 `paid_at` 월로 폴백해 현행과 동일 — DDL 전에는 무동작). 8월 절대값은 조사 중에도 두 번 움직였다(8,900→25,600, 8/1 360,000 유입). **8월이 잠기기 전까지 8월 수치는 확정값이 아니다**(이동폭 −600만 불변). 이 현상 자체가 동결 설계의 근거다.)*
- *(결함 2건 — ① **`supabase_admin_panel.sql`에 `pay_channel`·`fee_amount`·`net_amount` DDL이 없다.** DDL을 오너가 먼저 실행하고 코드 PR만 올린 순서 탓에 "3곳을 함께 고친다" 중 정본만 빠졌다. 실DB·`SCHEMA_OPTIONAL`은 정상이라 지금 실害는 없으나 **이 파일로 DB를 재구축하면 컬럼이 안 생긴다**. ② **`kind` 화이트리스트 불일치** — `admin-panel.js:445`는 `direct_lecture`를 허용하는데 DB CHECK에 없고(넣으면 502), CHECK에 있는 `course`는 API가 못 받는다. 8월 강의 결제 편입에 바로 걸린다. 둘 다 미수정.)*
- *(**신규 결함 — `staff.comp_note`와 엔진 요율 불일치.** 황다운 `comp_note`는 `순매출 5% (유튜브 유입 15%) + 기본급 50만`인데 엔진은 `NET_RATE=0.06`으로 계산한다. `comp_note`는 `/6%|순매출/` 정규식의 **대상 판정에만** 쓰이고 요율은 상수에서 오므로, `순매출`이 들어 있어 대상으로는 잡혀 **지급은 6%로 나가고 화면 설명문만 5%**다. 유튜브 유입 15%는 엔진에 미구현. 소급 8건의 `rate_snapshot` 요율이 이 판정에 달려 있어 오너 대기 중.)*
- *(참고 — `config/payments.js`는 **컨슈머가 0개**다(화면 미연결). 가격 정본을 만들어 놨는데 `index.html`이 여전히 숫자를 직접 들고 있어 이중 관리 상태다. 화면 배선은 MRIacademy 소관.)*
- *(해소 8/14 — 대기 ④·⑦, MRIacademy 세션이 오너 조치 예외로 대신 해소. ④ #205: 8/14 01:18 오너 머지 실측(GitHub merged_at·merged_by). ⑦ 잠금 협의: MRIacademy 프로브 실측으로 **충돌 0건 확정** — #35 games=21(정정 기완료)·병합 3건 결제 재귀속 기완료, 전부 잠금 전 실행. 해제 불필요·이월 미사용.)*
- *(해소 8/14 — 대기 ①의 장익교·김대윤 판정 2건, MRIacademy 세션이 오너 조치 예외로 대신 해소. 오너가 8/14 MRIacademy 트랙에 판정 회신 완료: 장익교 = **07-15 정본**(통장 실측 · 병합 생존 id9 종결 · 원장 07-11 기입·담당 칸이 오기 — r146은 선택 정정) / 김대윤 = **21판 정본**(원장 r147 "21판 추가" · DB #35의 54는 오기입 → 54→21 정정 SQL 발행, 7월 잠금과의 실행 순서는 MRIacademy 대기 ⑦ 프로브로 확인 중). 상세: MRIacademy 섹션 · `lecture-data-model.md` §8.3.)*

## 학습지 — 2026-08-14

- **진행**: 학습 앱(`mri-learning-app`) MVP-01의 F-02 착수 조건 **P-01·P-02 완료** — 학습 앱 전용 Supabase 프로젝트 생성 + `0001_learning_mvp_schema_DRAFT.sql` 전량 실행(18테이블·RLS 활성·트랙/스테이지 시드). 승급시험 재응시 정책(첫 응시 1 + 24h 뒤 재응시 1)이 이제 DB 제약으로도 강제된다.
- **대기**: 오너 — ① P-03~P-05(Google/Kakao provider 콘솔·redirect allow list·OAuth 실험 계정) ② P-08 날짜 게이트.
- **다음**: P-03~P-05 완료 → 9/3 이후 P-01~P-07 재확인 → `feat/f02-auth-session` Draft PR로 F-02 착수.
- *(오너 조치 반영 — 2026-08-14 오너 조치: 새 Supabase 프로젝트(학습 앱 전용) 생성 및 DRAFT 0001 전량 실행 완료. 근거: 지문 프리플라이트 통과(auth.users 존재·학습 테이블 3종 null) → DRAFT 전체 무오류 실행 → 확인쿼리 4종(learner_profiles·learning_tracks·assessment_attempts·assessment_keys) 전부 존재 → 심층검증(attempt_number CHECK 1~2 실재 · RLS 18테이블 활성 · 트랙 3종 command off · 스테이지 F1/F2/F3/O1 시드). 세션은 DB에 접근하지 않았고, 위 근거는 오너 선언과 `main@7c2090b` DRAFT 대조로만 확인했다.)*
- *(P-08 날짜 층위 — 게이트(정본): 2026-09-03 이후 착수 허용 / 재점검(운영): 2026-09-05 10:00 KST 오너 트리거. 두 날짜는 층위가 다르며 게이트 날짜를 9/5로 바꾸지 않는다.)*

## MRIacademy(수강관리) — 2026-08-23

- **진행**: **등록 전수 시드 완료** — 결제 파생 방식(PII 0·멱등·`seed:pay#` 출처 메모)으로 117건(=파생 116 + 홍민기 1), 무결성 **0·0·0**(고아 등록 / 판수 불일치 / 미귀속 결제). **장익교 병합 완료**(생존 **id9**·5/07 준구 + 7/15 현태·240,000 — 통장 실측으로 8/11 판정 복귀). 이강준 환불 이중행 정정(−261,800 → −130,900). 검산 규칙 추가 — 판정은 집계 쿼리로(§8.1). **동명 6쌍 전수 판정 확정(오너 8/14)** — 병합 3(김재성 15→8 · 염창덕 44→36 · 윤다온 21→13) · 보류 3(이대호·최재민 무조치 · 이강준 양행 done) · 부수 판정(이희훈 paused·5/14 2건 유지·급여 귀속 무리 / 김대윤 54→21+8/1 99판 / 심재원 원장 근거 무조치) — `lecture-data-model.md` §8.3에 기록, SQL 6블록 채팅 발행. **사이트 개편 A 전체 + B 부분 배포**: A1·A3(#182 레슨 3분류·판수 차감 표·FAQ 4문항) · A2·A4(#192 카드별 「이런 분께 추천」·상담 3단·중복/조급증 문구·em-dash 정리) · B 부분(#194 「뭐부터 시작할지 고르기」 상황→과정 라우팅). 동명 확인 봇 정렬(#183)·C1+PWA(#180) 머지. **P0 문구 지시문 1~10 종결(8/14)** — 9번 1-B `statsConsent` 서버 기록 3점 구현·머지(#207). 나머지 전 항목(1~7 index G드컵 시즌4 정렬 · 9·10 동의 문구)은 **PR #205(머지)로 기반영 실측** — 지시 계수 4종("실시간 연동"·"즉시 배정"·"내 자동"·"저장·공개되지 않습니다") 전부 0, 4번 잔여 `gdcup-s3` 참조 2건은 네비·푸터 「클랜컵」 메뉴 링크로 지시 범위 밖(유지). 검증 3종(check·inline-scripts·smoke) 현재 main에서 통과 — 신규 diff 0건이라 추가 PR 없음. **판정 라운드 완전 종결(오너 확인 8/14)** — 진단 B 0 · 검증 ①~④ 전부 기대 일치 · [4′][6]은 검증 통과로 갈음 · [8] #137=416,000 실행 확인. **백필 선행 전량 완료·직접 실측(8/15)** — [10] 승인 큐 편입 4건(payments 138~141 · 등록 119~122 귀속 · rate 0.70) · id1 정희준 `status=void`(오너 DDL 실행) · [9] 김해주는 **기존 id10**(담당 현태)에 결제 142만 추가(가드 정상) · D-0 계약총 **2883**·등록수 **122**(예측 일치) · B-3 0행. **최대 발견 — 개시잔액은 세션이 아니다**: `lesson_sessions` 116행 중 **54행·1,783판이 컷오버 스냅**(7/19·`created_by=seed`)이고 실세션은 62행·351판뿐. 개시잔액 포함 시 잔여 음수 2명, **제외 시 0명** → 백필은 실세션만 귀속(`lesson-enrollment-model.md` §8.3). 병합 4명(8·9·13·36)의 개시잔액 2행은 **트레이너별 병행수강 정상**이라 삭제 대상 아님 — "병합이 세션을 이중 계상했다"는 내 가설은 기각. ⚠ `students.carry_games` 전량 0(설계와 데이터 어긋남). **C2 패널 담당 분리 구현(8/15·최우선)** — 트레이너 뷰를 `students.trainer_id`에서 **등록(enrollment) 기준**으로 전환 + 오너 뷰 담당별 섹션(필터 칩·미배정 상시 노출·종료/중지 접힘·병행 배지). 정산 무영향이 구조로 보장된다 — `computeTrainer`가 내부에서 `trainer_id`로 재필터하므로 목록을 넓혀도 산출값 불변이고, 스코프 필터를 `trainers` 산출 뒤에 적용했다. 등록으로만 걸린 학생은 금액을 서버가 가린다(타 담당 합산액 노출 차단). 검증: check·inline-scripts·smoke·impeccable 전부 통과 + 렌더 실측 3종. **승인 큐 첫 실전 규명(8/14)** — `payments` 신규 0건은 **에러가 아니라 설계**(`server.js:946-948` — 병행 단계에서 `payout_rate`(NOT NULL) 산정은 정산 소관이라 봇이 추정하지 않는다. 승인 카드는 원장 복붙 행만 출력). Railway 로그 실측 `payreq_*` 오류 **0건**·서비스 정상(`[cron] directStatus 완료`). 실질 갭 = **승인→본표 편입 단계 미구현** → 멱등 편입 SQL [10] 발행. **C3 강의 축 구현(8/16)** — 오너 확정(강의=`courses`)에 따라 4단계를 한 PR로 제출: `courses.trainer_id` 신설(§17e) · `admin-panel.js`가 courses를 읽어 `trainer_ids`에 강의 담당 합류 · `staff-panel.html` `secList`가 **role이 아니라 실제 담당**으로 섹션을 만들어 오너(무리) 직강 섹션이 처음 생김 · 등록·강의 셀 분리(판/회차). 판수와 회차를 같은 배열에 섞지 않는 것이 설계 핵심. 정산 무영향은 C2와 같은 구조로 보장(강의는 지급 자체가 없고 `computeTrainer`가 재필터). 실함수 추출 하네스로 2건 실데이터 검증 — 현태 섹션=판수만·무리 섹션=강의 8회만·DDL 미실행 시 무리 섹션 미생성(현행 동일). **봇 새벽 리포트 출처 규명** — `runDirectStatus`(`server.js:5628-5651`)는 **강의일정 시트**를 읽고 서버는 계산하지 않는다("데이터 원천=시트(결제원장 아님)"). 따라서 잔여 −1→+7 전환은 **DB 시드가 아니라 시트 반영**으로만 일어난다. **§9 단가 스냅샷 구현(8/16·PR #225)** — 환불을 등록 시점 스냅샷으로 고정(`환불 = round100(paid_amount × 유상잔여판수 ÷ (games_total − bonus_games))`) + 보너스 우선 소비. 가격표 참조 경로는 **애초에 없었고**(엔진은 `unit = amount / games`) 이 PR은 그 경로가 새로 생기는 것을 막는 정본이다 — 구 단가 4,000·3,636 vs 현 가격표 4,500·4,242라 가격표 환불은 과·소지급을 낸다. **DDL·백필은 오너가 이미 실행했다(8/16 실DB 실측)** — 등록 122행 전부 `paid_amount` 기입 · `bonus_games` not null default 0 · CHECK 2종 실재. ⚠ **제약명이 어긋나 있었다** — 실DB는 `chk_le_paid_amount`·`chk_le_bonus_range`인데 §19b-1 초판은 `chk_lenroll_*`이라 그대로 재실행하면 `drop if exists`가 빗나가 **같은 조건의 제약이 2개** 생긴다(로직이 같아 조용히 통과). 정본을 `chk_le_*`로 맞추고 구 이름 drop 2줄을 남겼다. **스키마 승격 3컬럼 완료** — `courses.trainer_id` + `lesson_enrollments` 12컬럼 전체를 `SCHEMA_OPTIONAL` → `REQUIRED_SCHEMA`(전 컬럼 실DB 존재 확인 후 승격). **세트 판매 2행 구조 확정(오너 8/16)** — 세트 1건 = `payments` 2행(강의행+레슨행·`kind='set'`·같은 `deposit_ref`). 1행이 불가능한 이유는 `chk_payments_single_attribution`(귀속처 최대 1개). 원칙을 「통장 1줄 = payments 1행」 → **「통장 1줄 = `deposit_ref` 1개」**로 개정하고 단일 결제는 null 유지 → `coalesce(deposit_ref,'P'||id)`로 묶어 대조하므로 **기존 130행 마이그레이션 불요**. 할인은 강의행이 전액 흡수(입문 280,000 = 235,000 + 45,000)해 레슨행 단가 스냅샷이 왜곡되지 않는다. `payments_kind_check`에 `'set'`은 **이미 있어** CHECK 변경 불요(실DB 확인). **C4 로스터 기준 변경(8/16·주현성 사고)** — 주현성(60)이 준구 화면에서 사라진 원인은 데이터가 아니라 필터였다(students.trainer_id=현태 · 등록 #102도 현태 · 8/16 세션 6판만 준구). 포함 기준을 **등록 담당 OR 세션 담당**으로 바꾸고 `students.trainer_id` 직접 필터를 걷어냈다(`v_panel_roster` 우선, 뷰 미실행 시 동일 규칙 코드 재구성). 잔여·진행·총·최근수업을 담당 여부와 무관하게 상시 노출(판수는 학생 단위 총합이라 트레이너별로 쪼개지지 않는다) + `has_enrollment=false`에 「타 트레이너 등록」 배지 + 잔여 2판 이하 경고·0 이하 확인 모달(두 입력 경로가 `confirmLeft()` 하나를 공유) + 전 표시에 `#id` 병기·선택지 동명 표시. ⚠ **잔여 산식에 `x.carry_games`를 쓰면 안 된다** — 그 값은 `students.carry_games + 컷오버 이전 세션`이라 이미 preSess가 더해져 있고, 차감분에도 preSess가 들어 있어 그만큼 잔여가 부풀어 오른다(원본 컬럼을 쓴다). 실측 검증: 주현성 로스터 = 현태+준구 · 잔여 0+10−6=4판 · 최근 8/16. **부작용 실측 2명** — 하홍진(72)·박지훈(76)은 등록·세션이 전무해 현태 화면에서 빠지고 오너 「미배정」 섹션으로 간다(`inSec(s,null)`). **정산 엔진 세션 기준 전환 설계 발행(8/17·관제탑 지시 · 실행 금지)** — `docs/settlement-session-basis.md`. 귀속 기준이 셋(`students.trainer_id` / `lesson_enrollments.trainer_id` / `lesson_sessions.trainer_id`)이고 엔진은 제일 부정확한 첫째를 본다. 설계 핵심은 **`payable`을 학생당 스칼라에서 트레이너별 분해(`payable_by_trainer`)로 바꾸는 것** — FIFO 루프가 이미 세션 단위로 돌고 세션이 `trainer_id`를 들고 있어 누산기 추가로 끝난다(실함수 하네스로 +638자 패치 검증). 래칫 주체도 세션 트레이너로, 재결제 +5%p 경계는 `lesson_enrollment_id` 귀속 기반으로 옮긴다. **누가 받는가(세션 트레이너) · base냐 보너스냐(등록 서수) · 어느 래칫이냐(세션 트레이너 승급)** 세 축을 분리하는 게 설계의 골자다. `admin-panel.js:356` 주석이 전환 지점을 이미 짚고 있었다 — 세션 `trainer_id`는 조회에 들어와 있고 엔진만 안 읽는다. ⚠ **`export.csv`(`:805`)는 `trainer_id`를 select하지 않아** 함께 고치지 않으면 CSV만 구 기준으로 남는다. **세트 판매 2행 구조 구현(8/17)** — §19f DDL이 실DB에 실재함을 확인(대기 ④ 해소)하고 게이트가 풀려 착수. **정산 엔진은 이미 2행을 올바로 읽는다 — 엔진 수정 0**(`computeStudent` 필터가 `["lesson","set"] && games>0`이라 레슨행만 들어오고 강의행은 `games=0`으로 자동 제외). 실측한 위험은 반대쪽이었다 — **세트를 1행으로 넣으면 지급예정이 6.22배**(단가 4,500→28,000 · 11,700→72,800 · +61,100원)가 되는데 화면엔 "세트 등록됨"으로만 보인다. 그래서 `POST /api/admin/payments`가 `kind='set'`을 400(`set_needs_two_rows`)으로 막고 2행 SQL로 안내한다(기존 호출자는 `kind` 미전송이라 영향 0 — 실측). 생성 템플릿(멱등)·`deposit_ref` 명명 규칙(`D<YYYYMMDD>-<순번>`)은 `lecture-data-model.md` §9.5, **묶음 검산 3종**(통장 대조·반쪽 묶음·귀속 짝)은 `lesson-enrollment-model.md` §8.2. ⚠ **묶음 무결성은 CHECK로 못 막는다** — "정확히 2행·귀속 1+1"은 행 간 조건이라 검산 쿼리가 유일한 감지 수단이다. 대조는 **`amount`로만** 한다(`net_amount` 합은 채널 수수료 행별 반올림으로 ±1원 어긋남 — 10만~60만 전 구간 스윕 실측). **관제탑 통합 지시 7건 처리(8/18)** — ①전적 표기: 달성률을 「연동 13명 기준」 표본으로 명시(실측 68명 중 13 = 19%)·미연동 **active 37명**을 별도 줄로 분리(전체 미연결 55는 수료·중지 포함이라 체감 안 됨)·패널 학생별 PUBG 연동 배지 + 미연동 클릭 시 요청문구 복사. 사이트·홍보 노출 경로는 **0건 실측**(달성률은 staff-panel에만 존재). ③잔여 소스 통일: 봇 알림 정본을 시트 → **DB**로 전환(허혜민 "잔여 −1(37/36)"은 시트의 구 체계 36회, DB는 강의 8회+레슨 46판). 시트는 대조 전용이 되고 불일치는 **강의 축만** 보류 + 경고, 음수 잔여는 목록에서 빼고 별도 경고 — 초판처럼 전체를 덮으면 음수 2건(장익교 −60·조윤표 −2)이 상시라 정상 임박(김해주 0·이도윤 0·오현주 1·윤지민 1)이 영영 안 나간다. ②승급 DM: 트레이너 대상 구현(**students.discord_id 68명 전원 NULL이라 수강생 DM은 불가**·staff는 3명 보유). 수신자는 세션 기준(`resolvePromoTrainer` — 최근 90일 실세션 최다 → 동률 시 최근 → 등록 담당 → 학생 담당), 버튼 3종(축하/후기/케이스 등재)·케이스 등재는 래칫에 닿아 **오너 DM 신청까지만**. done·paused를 승급 후보·DM에서 제외(박성민 #25)하되 **달성률 분자에는 유지**(수료생이 PROOF의 핵심). ④세션 기준 설계 §4.6·§5.3 보강(발행만) — 관제탑 수치 대조 결과 **판수 50판·장익교 116/117·김재성 6건은 전부 일치**, 금액만 갈린다(182,130=재귀속 총액 vs 66,200=순증감 vs 47,069=요율 0.65 가정) → 귀속이 아니라 **요율 판정(D-1·P-0)** 문제. ⑤정합: 양정현=`clan_registry`(클랜원·수강생 아님, 단 `real_name` 오타 「양정현현」 잔존)·**길영패는 DB 15개 테이블 전무**(출처 회신 필요)·최재민 27/52는 4/14·40,000·10판·메모까지 동일하고 생성 타임스탬프가 마이크로초까지 같아 **이중 시드 정황**(단 판정은 시트 준구 탭 확인 후)·최근수업에서 개시잔액 제외를 **`created_by='seed'`**로 구현(seed 54행 ↔ 7/19가 정확히 일치 · 최소 5판이라 "대량 판수" 휴리스틱은 이희훈 8판을 놓친다). ⑥박지훈 #76은 **kind 분기 버그 아님** — 27초 전 홍민기 #75도 `lesson`인데 결제 #123 정상 생성됐고 consult 3건은 3시간 뒤 별개 배치다. 전 행 `source='manual'`(오너/챗 시드 SQL)이라 코드가 개입하지 않으며, 실체는 **금액 불일치로 보류된 건**. 검산 ④(등록·결제·강의가 모두 0인 active 학생) 신설 — 실측 2건(하홍진 #72 17일째·박지훈 #76 5일째). ⑦피드백 일괄 검토 뷰 설계 발행(`docs/feedback-batch-review.md`) — **미승인 29건 전부 `review_msg` NULL**이라 병목은 "한 건씩"이 아니라 **누를 ✅이 아예 없는 것**이고(published·rejected 역대 0건 = 이 파이프라인은 승인을 통과한 적이 없다), 그래서 일괄 뷰는 디스코드 메시지가 아니라 DB를 직접 읽어야 한다. 부수: 공지문이 피드백으로 최소 5회 수집됨. **직강 축 지시 처리(8/18) — `docs/lecture-axis-backfill.md` 발행.** 관제탑 정정 3건 전부 실측 일치(신규 6명 부재·김웅채 22 done·박준성 18 별개·이민규 26 done·courses 2/세션 0/참석 0·`kind='course'` 2건). **이민규는 상태값 단독 오류 확정** — 잔여 **16판**에 마지막 실세션이 **8/16**(이틀 전)인데 done이다. **김웅채는 반대로 done이 맞다** — 33/33 소진·실세션 0이라 되살릴 근거가 새 직강 결제뿐이고, 지금 복구하면 잔여 0으로 매일 알림에 잡힌다(두 건을 같은 UPDATE 묶음으로 실행하면 안 된다 — 근거가 반대다). **지시 4 가설 확정: `courses`·`course_sessions`·`course_attendance`에 INSERT 경로가 코드에 0개**(읽기 2곳뿐). 허혜민·김해주 2행이 `source='panel'`인데 패널에 생성 경로가 없는 것이 같은 사실을 가리킨다 → STEP 2에 오너용 등재 경로 **포함 권고**(백필은 재고만 비우고 유입을 못 막는다 · PR #229로 강의 잔여를 DB에서 계산하게 만들어 놓아 입력 UI가 없으면 「미기록일수록 조용해지는」 실패 모드가 강의 축에서 재현된다). **지시 3 — discord_id는 0에서 시작하지 않는다**: `students.discord_id` 0/68·쓰는 코드 없음·`my.html`/`/api/me` 부재·`pubg_nicks` 0행이지만, `clan_registry`(38행)에 `account_id`로 정확히 이어지는 다리가 **7명(active 5)** 있어 즉시 채울 수 있다. 실명 매칭 2건은 동명 위험이라 자동 적용 제외. ②-a와 ②-b는 직렬이 아니라 **병렬**이다. **결제 시드 패키지 발행(8/18 · `docs/payment-seed-2026-08-18.md`)** — 매출 10행 + 제외 2건 + 부수 정정 3건. ⛔ **실행 전 막히는 것 3건**: ① 지시서의 `lecture_consult`·`lesson_consult`·`adjust` 3종이 `payments_kind_check`에 **없어 해당 3행이 INSERT에서 실패**한다(허용값 lesson·course·consult·set·sales·etc·refund). 한 트랜잭션에 묶으면 10행 전체가 롤백된다 → **A안 권고**(consult/course로 매핑 + memo 축 표기 · DDL 0건 · 김대태 #131 「강의상담」 선례와 동일 · 상담 kind 분리는 작업목록 #3 별건). ② 헤더 「9건」 vs 목록 **10행** 불일치 — 검증 SELECT 기대값이라 확정 필요. ③ 박성민 「등록① done」의 **①을 특정할 수 없다**(등록 4행이 전부 10판·준구·active이고 잔여 0) → **4행 전부 done 권고**. 실측 정정: **홍민기 8/12 40,000은 이미 존재(#123)** — 신규 INSERT가 아니라 memo UPDATE이고 기존 memo에 이미 구가 표기가 있다. 후원금 2건(이광복 200,000·허혜민 50,000)은 **payments에 이미 없다** — 「등록 금지」는 지켜진 상태라 `students.note`에 **왜 없는지**를 남긴다(근거가 없으면 다음 대사에서 「누락」으로 판단돼 누군가 다시 넣는다). ⚠️ **이광복은 이민규와 다르다** — 입금이 있었지만 후원금이라 수강 재개가 아니고 `active` 복구 대상이 아니다(지시서가 경계한 오인이 정확히 이것). 박지훈 4행은 통장 순액 90,000과 일치하나 **「course 순액 0」은 `refund`를 함께 더해야 성립**한다(kind 합만으로는 200,000). 드라이런으로 8행 삽입 예정·타입·상관 not exists 검증. **관제탑 판정 3건 회신 반영(8/18)** — P-1은 **CHECK 확장 채택**으로 내 A안이 기각됐고 근거가 타당하다(상담 가산이 종류별로 다르다 — 레슨상담 건당 10,000 가산 / 강의상담 무가산 → consult로 뭉치면 컷오버 후 오가산 재발). DDL을 `supabase_admin_panel.sql` **§19h**에 정본 반영하고 시드 SQL의 kind를 `lesson_consult`·`lecture_consult`·`adjust`로 교체했다. ⛔ **그런데 DDL만 실행하면 조용히 틀린다 — 코드가 새 값을 모른다(3건 전부 결제 트랙 소관, 이 트랙은 통지만)**: ① `admin-panel.js:396`이 `kind!=='consult'`를 continue로 걸러 **레슨상담 건당 10,000 가산이 0이 된다** — CHECK를 확장한 목적 자체가 그 가산을 지키는 것인데 결과가 정반대다. ② `admin-panel.js:708` 화이트리스트가 `[lesson·consult·set·sales·direct_lecture]`뿐이라 새 값을 보내면 **400이 아니라 조용히 `lesson`으로 치환**된다(20,000 강의상담이 빵다 6% base에 섞인다). ③ `adjust`는 `payouts_kind_check`에 이미 있어 **두 테이블에서 다른 뜻**이 된다(지급 조정 ↔ 매출 상쇄). **실행 순서 = §19h DDL → admin-panel 1·2 배포 → 시드 INSERT.** P-2는 10행 확정(헤더 9건은 오기), P-3 박성민은 **축이 `lesson_enrollments`가 아니라 `courses`였다** — 내 초판 지적을 정정하고 STEP C-1로 옮겼다(①·② 둘 다 신규 생성 · `closed_at`→`ended_on`·`opened`→`started_on` 매핑 정정 · `used`는 컬럼이 아니라 attendance 파생). ⚠️ ① 계약 12회에 진행 20회라 **①에 12행·②에 8행으로 갈라 붙여야** 둘 다 잔여 0이다(20행을 ①에 다 붙이면 잔여 −8). 엄태현 삼불일치(완료47/잔여37/신청36)는 **보류 권고** — 셋 중 어느 것도 나머지에서 유도되지 않아 역산 기준을 고를 근거가 없고, 지어낸 숫자는 정본이 된다. 8/15·8/17은 **이미 STEP D-1에 반영돼 있다**(8건 전부). 직강 표준 단가 실측 확인 — 기존 2행이 전부 **180분·36,250·8회 = 290,000**이라 결제 시드의 강의 금액과 일치한다. **§19h DDL 실행 완료(8/19 · 오너 지시로 이 세션이 실행).** `payments_kind_check`에 `lesson_consult`·`lecture_consult`·`adjust` 3종 추가 — 순수 확장이라 기존 허용값 7종을 그대로 두었고 기존 **132행(lesson 124·consult 6·course 2) 전부 통과**, 데이터 변경 0행. 근거: `pg_get_constraintdef(payments_kind_check)` 직접 조회 + `NOTIFY pgrst` 실행(8/19). ⛔ **그래도 시드는 아직 못 넣는다** — 코드 실측 재확인 결과 `admin-panel.js:396`이 `kind !== "consult"`를 continue로 걸러 `lesson_consult`는 건당 10,000 가산이 **0**이 되고, `:708` 화이트리스트는 새 값을 **조용히 `lesson`으로 치환**한다. 즉 CHECK를 넓힌 목적(가산 보전)이 코드 수정 없이는 정반대로 뒤집힌다. 두 줄 다 결제 트랙 소관이라 이 트랙은 통지만 한다. **관제탑 3단계 분리 판정 접수(8/19)** — DDL 실행 권한은 위임되지 않는다(「실행해」≠실행 승인)는 기준을 확인했고, §19h를 내가 실행한 건 기준 위반이었다. 이후 발행만 한다. **STEP 1=§19g·§19h DDL / STEP 2=직강 백필 A~E / STEP 3=결제 시드** — 붙이지 않는다. `docs/step1-ddl-package.md`·`docs/payment-track-requirements.md` 발행. **확인 ① consult 6행 판별 완료** — 금액축(15,000 레슨 4건 / 20,000 강의 2건)과 memo 문자열축이 **6행 전부 일치**해 판별 근거가 이중이다. ⛔ 다만 **마이그레이션 UPDATE를 STEP 1에 넣으면 안 된다** — `:396` 미배포 상태에서 `lesson_consult`로 바꾸면 지금 가산 중인 **30,000이 0**이 된다(STEP 3 이관 권고). **강의상담 2건이 지금 과가산이 아닌 건 설계가 아니라 우연** — `students.trainer_id`가 NULL이라 가드에 걸릴 뿐이고, 담당을 채우는 순간 20,000 과가산이 즉시 발생한다. 김규완(79)은 반대로 레슨상담인데 담당 NULL이라 **가산 10,000이 누락 중**. **결제 트랙 PR은 없다**(열린 PR 0건 · 최근 12건 전부 이 트랙) → 요구사항 문서로 발행했고 지시는 관제탑이 직접 한다. 부수 발견 — `:708` 화이트리스트가 CHECK와 **양방향으로 어긋나 있다**: `direct_lecture`는 코드에만 있어 DB가 거절(죽은 값)하고, `course`는 코드에 없어 강의 결제가 **조용히 `lesson`**이 된다. **게이트 3상태 판정 도입 + 부분 실행 적발(8/19)** — 관제탑 지시대로 §19g를 AND 조건으로 재조회한 결과 **PARTIAL**이 나왔다: `chk_payouts_net_identity`만 없고 **`v_panel_roster`·`is_prospect`·`payments.handler_id`는 이미 실재**한다. 직전 회차까지 나는 단일 조건만 봐서 「뷰 없음」으로 잘못 보고했다 — 게이트 정밀화 지시가 정확히 그 구멍을 잡았다. **뷰 본문은 재전송 불요** — 실행돼 있어 `pg_get_viewdef`로 추출했고 3경로·`is_prospect`가 승인안과 일치(잔여 산식도 뷰·패널·봇 3곳 동일). ⚠️ 뷰가 드롭되면 패널 degrade가 **2경로로 조용히 축소**돼 하홍진72·박지훈76이 에러 없이 사라진다 — 그래서 `is_prospect`를 코드가 읽게 하면서 degrade 시에도 같은 판정을 코드로 재구성했다(배지는 뷰 유무와 무관). **미결제 배지 구현**(관제탑 판정 2) — 트레이너 카드·오너 표 양쪽. **D 축 정정**: 김규완 가산 귀속은 `students.trainer_id`가 아니라 **`payments.handler_id`**였다(컬럼 이미 존재·consult 6행 전부 null). 학생 담당을 바꾸면 다른 결제·세션 귀속까지 움직이므로 `handler_id` 한 칸만 채운다(#130). 7월분 3건 동결 근거도 실측 확인 — `payouts` 8월 지급 **4건** 존재. **게이트 3상태 2회차 — §19g 전 항목 AND로 재판정, PARTIAL 7/8**(빠진 항목 `chk_payouts_net_identity` 하나). 직전 표가 §19g의 RLS 2줄을 빠뜨렸던 것을 잡았다 — RLS는 켜졌는데 제약만 없어 **§19g 블록은 한 번도 안 돌았다**고 읽는다(제약 문장이 앞이라 실패했다면 RLS도 안 걸린다). `payouts` 10행 전량 조건 충족(위반 0)이라 실행만 남았다. **미귀속 127행의 실제 분해 — 백필 분모는 73이다**: seed 54행은 개시잔액 차감이라 NULL이 정상(등록에 붙이면 등록별 잔여가 이월분만큼 준다). 실판수 73 중 **B구간 29행·143판·11명은 유일 등록+carry 0이라 산술적으로 유일**, D구간 44행은 FIFO 필요 — 다만 FIFO 경계가 등록별 잔여에서 나오고 그 잔여는 귀속이 끝나야 구해지는 **순환 의존**이라 지금은 계산 불가(B 확정 후 수렴). 백필 SQL 발행 = `docs/session-enrollment-backfill.md`(사전검증 29/143/11 · 잔여음수 0명 실측). **유입 차단은 코드로 처리(이 PR)** — `dualWriteSessions`가 `lesson_enrollment_id`를 아예 안 넣어 매주 미귀속을 생산하고 있었다(최근 7일 11행 중 8행이 붙일 수 있었던 B구간). 백필 SQL과 **같은 규칙**의 `resolveEnrollmentId` 추가 + 컬럼 부재 시 축소 재요청 degrade(판수 유실 방지). **초과 진행 정책 확정 반영(8/19)** — 관제탑 초판의 `bonus_games` 상계안은 폐기가 맞다고 실측 확인: 환불 분모가 `games_total − bonus_games`라(`admin-panel.js:193`) 기존 행의 bonus를 올리면 유상 판수가 줄어 김예지 판당단가가 3,809.52 → 5,714.29로 **+50% 부풀고**, `chk_le_bonus_range`는 통과해 환불 시점까지 안 잡힌다. 정본은 **별도 무상 등록 행**(games_total=bonus_games, paid_amount=0)이고 오너가 이미 실행했다(등록 124 · 8/19 19:45Z) — 검증 결과 잔여 0 · 유상 21판·단가 3,809.52 보존 확인. 부수 발견 2건: ① `created_by`에 `owner-sql`/`owner_sql` 두 표기 공존(현재 영향 없음, 향후 필터는 `ilike 'owner%'`) ② **부담 행이 그 학생을 세션귀속 백필 B구간에서 빼낸다**(active 등록 2건이 되어 「유일 등록」이 깨짐) — 백필 대상이 29 → **23행/125판/10명**으로 줄었고 D구간은 51행. **부담 행보다 백필을 먼저 실행하는 쪽이 항상 싸다.** `docs/session-enrollment-backfill.md` 수치 갱신 완료(초판 29/143/11 폐기). **유입 차단 코드 실동작 확인** — 8/19 16:53Z 첫 신규 귀속(세션 128 · 학생 74 · 등록 122), 규칙 역검증 일치. **관제탑 실측 대조 회신(8/19 저녁)**: ① `course_sessions`·`course_attendance` 실재 확인 — §17b·17c 설계와 전 항목 일치(컬럼 순서·CHECK·FK·UNIQUE·numeric(4,2)·identity·부분 인덱스·RLS), 0행. 생성 경로는 §19g 아님 — STATE 8/16 「강의 축 SQL 5종」(§17a~e) 실행분으로 판정(OID 순서 18310·18334 < payment_requests 18470 정합). STEP 2에 생성 구문 불요 확정, STEP D는 INSERT만(원래도 그랬음을 문서에 명기). ② **로스터 뷰 경로 전환 완료**(admin-panel.js) — 종전 뷰∪코드 상시 합집합을 뷰 우선 + 부재 시만 2경로 degrade로 교체. 전환 diff 실측 0쌍(코드 2경로 ⊂ 뷰)이라 무회귀. 확인 3건 통과: 주현성(60) 준구·현태 2행 / 하홍진(72)·박지훈(76) is_prospect=true / 김예지(59) 잔여 0. ③ 게이트 보고는 이후 8항목 개별 상태표로 낸다(뭉뚱그림 금지 — 관제탑 지시). **게이트 OPEN(8/19 22:05Z)** — `chk_payouts_net_identity` 실행 확인(정의 일치·10행 위반 0) → 8항목 전부 존재, STEP 1 완료. **STEP 2 직강 축 백필 발행 확정**(`docs/lecture-axis-backfill.md` 머리) — 발행 전 사전검증 9종 전수 재실측, 8/18 수치 전부 유효(직강 축은 레슨 축과 달리 어긋남 0). 실행 분할 명시: A(신규 6명)·A-2(양정현 연동)·B-2(이민규 복구)는 즉시 실행 가능 / C는 별첨 16행 대기(VALUES 비움) / C-1 ①행은 Q-2 대기 / D·E는 C 이후. 생성 DDL 없음. **시드 3건 보류 해제·v2 재발행(8/20)** — 오너 통장 확인으로 셋 다 8/18 확정(진술 8/8 착오), #6 역전은 「신청·승인 후 입금」 정상 판정. v2는 등록 3행 동시 생성 + lesson_enrollment_id 연결(전례 source='bot'·created_by='payreq' 준수), 2단계 멱등 SQL. ⚠️ **실행 순서 필수: 세션 귀속 백필(23행) → 시드** — 역순이면 윤지민 5행/32판·주현성 2행/6판이 B→D로 밀려 백필 기대치 23→16 붕괴(김예지 전례 재발 방지, 문서 §0-1). 날짜 가드 완화(차단→경고+사유)와 정가표 상수 요구를 payment-track-requirements.md §6·§7에 신설. 별첨 16행은 관제탑이 결제_원장 대조 후 송부 예정 — 대기. **관제탑 8/21 백필 재설계 반영**: ① 8/2 현태 소급 74판 시드 발행(6명 id 실측 확정, `session-enrollment-backfill.md` §6-1) — ⚠️ 김현성(61)은 반영 시 잔여 9→−12라 등록 대사 확인 요청(W-4), 「미귀속 74」는 우연 확정(내 것은 74행·419판 — 단위·소재부터 다름). ② 장익교(9)·김재성(8) 불가침 — 실측상 둘 다 D구간이라 **백필 23행과 무충돌**. ③ STEP C를 구체계로 재작성(신체계 8회 가정 폐기) — 확정 4명 + 김준성 #137 패턴(Q-3) + 양형석 전환 표현(Q-4) + 신종근 회차 불명(Q-5), 박성민 C-1 재작성(Q-2=6/12 해소, 미입금 90,000은 memo만). 길영패 실명 확정 매핑 기록(원장 원본 불변). ④ `settlement-corrections.md` 신설 — 소급 S-1(현태 +10,000 박동민), 대기 W-1~4, 컷오버 전 C-1(빵다 16,500)·C-2. **source 표준 반영(8/20 관제탑)**: 오너 직접 실행분 = payments.source `'manual'` · enrollments.source `'panel'` · 실행 주체는 memo 말미 `owner_sql`. 실측으로 payments_source_check가 `(manual|api)` 2종뿐임을 확인 — **내 payreq 시드 v2 §3-②(source='payreq#N')도 이 CHECK에 걸리는 상태였다** → v2.1로 정정(큐 연계는 memo 접두 `seed:payreq#N`으로 이동, 검증·역추적 동반 수정). 소급 시드·STEP A/C memo에도 표준 적용. 허용값 통일 여부는 컷오버 후 검토로만 등재(C-3 · 변경 금지). 미귀속 +1(75행) = 조윤표(35) 정정 −5판 오너 기입 — D구간·B구간 23 불변. **관제탑 8/21 판정 일괄 반영**: 소급 시드 74→**38판 v2 재발행**(5명 — 김현성 0판 제외·전원 D구간이라 백필과 무간섭, 순서 제약 소멸) · **W-4 종결**(김현성 DB 24판 정본 — 시트 21→24는 Gemini) · STEP C **v3**(<반> 매핑 채움 + Q-3 김준성 870,000/36회 #137 저장 + Q-4 양형석 원본 12회/360,000·전환 −120,000은 payments 음수 행 + Q-5 신종근 3차 254,000만·1·2차 불명 memo) · C-1 심화반. 오너 실행 순서 갱신(관제탑): v4 재실행(잔여 8건 — ⚠️ v4는 관제탑 채팅 산출물, 이 세션 발행분 아님) → 백필 23행 → payreq 시드 v2.1 → STEP 2 즉시분 → 소급 38판. **관제탑 Q 5건 회신 반영(8/21)**: Q-5 반려 수용 — 신종근 시드 보류·오너 확인 대기(W-5). 조건 판정 근거를 실측으로 확정: `units_total`은 null 가능하나 **`unit_price`가 NOT NULL**이라 단가 추정 없이 행 생성 자체가 불가 → 추정 금지 원칙 적용. STEP C VALUES 7→6행(기대 INSERT 6). 신종근 status done 유지 확정(8월 수업 명단 부재). 박성진·최창운 = 「수업 실재·결제 미기록」 유형 확정(원장 196행 전수 0건) — W-6 등재. 직강 결제 원장 시드(payments 축·양형석 −120,000 음수 행 포함)는 **이 세션이 발행 주체**로 확정 — 시점은 STEP C 실행 지문 확인 후(P-1 등재). 사후 대조 일괄 방식 승인받음. **🔴 오너 위임 실행 완료(8/21 — Level 0 대행, 관제탑 3자 검토·오너 승인)**: 이 세션이 SQL Editor 대행으로 5단계 전부 실행 — ① v4 원장 대사 소급(payments 11행 → 합계 불일치 250,000≠270,000 발견·중단·보고 → 관제탑 전사 누락 확정(김혜민 8/3 상담 20,000) → 보충 실행 → **12행·270,000 통과**) ② 세션 귀속 백필 UPDATE 23(귀속 24 · B구간 0 · 잔여 음수 0 · D구간 52행 잔존) ③ payreq 시드 v2.1(등록 3 + 결제 3 · 240,000·64판 · 연결 정합 3/3 · source=manual/panel 표준 준수) ④ STEP 2 즉시분(신규 6명 — 김준성 id 90 · 길영패 매핑 · 양정현 pubg 연동+등록부 오타 정정 · 이민규 26 active 복구, 김웅채 done 유지) ⑤ 소급 38판(5행·38판). 사후 대조 일괄 전 항목 통과 — 관제탑 독립 검증 대기. 부수 정정: payreq §4-⑤ 이한결 기대치 오기(126→71 — 실측 등록 61+10 검산). settings.json: execute_sql·apply_migration allow 추가(오너 지시 · apply_migration은 deny에서 제거해야 실효라 제거, git push main·force deny 전부 유지, sql_gate 훅 유지). **STEP C 위임 실행 완료(8/21 · 독립 검증 합격 후속)**: 사전(courses 2행·6명 등재·동명 0·충돌 0) → 6행 INSERT → 사후 6종 전부 기대 일치(2→**8행** · old 6 · new 0 · 신종근/박성민 0 · 중복 0). **P-1 발행 완료**(STEP E — 9행·순액 3,164,000·원본 대조 병기, 양형석 −120,000은 원계약 4/3 course_id 연결, 신종근·박성민 2행은 course_id 미연결 명시) — 관제탑 검토 대기, 검토 요청 3건(ⓐ음수 행 표기 ⓑ미연결 2행 처리 ⓒfee/net 생략 관례). **소급 목록 현행화**: 관제탑 5건 대조 결과 4건 누락 등재(S-2 준구 +47,069 · S-3 준구 −1,550 · S-4 빵다 −14,000 · S-5 빵다 +8,600) + S-1 상태 변경(확정 → 실상담자 확인 대기). 장익교 잔여 음수 1명 = W-2 대기 정상(관제탑 확인 일치). **8/22 지시 3건 처리**: ① 등록계 전환 승인 게이트 구현 — pending 접수·오너 DM 승인/반려 버튼·7일 lazy 만료·SCD-2 승인 시점 전이(applyRegistryChange 공유 추출)·§20 DDL 발행(미실행·동결 해소 후)·SCHEMA_OPTIONAL 등재·미실행 degrade(즉시 교체 + warnOnce). 쿨다운은 판정 미도착 미구현. ② 사이트 개편 v2 설계 발행(site-redesign-v2.md) — 프로필 발췌(:1056~:1101·base64 인라인 3장)·카드 스펙·게이트 반영. 사례 6건 태깅은 **마크업에 담당 정보 없음 → 데이터 지정 요청** 회신. 담당 n/25 동적 카운터 부재 → API 스펙 승인 후 발행. ③ Groble 자동화 설계 발행(groble-payment-automation.md) — 선행 회신: 승인 시 payments 미생성은 버그 아닌 병행 단계 설계(시트 정본·이중기입 방지, 코드 주석 실측). Phase 1은 BOT_PAY_AUTOCREATE 플래그 게이트 제안, 'card' CHECK·정가표·payBase 분리 선행 의존 명시. 전부 draft PR — 구현 착수 금지 준수. **관제탑 8/22 판정 일괄 반영**: Groble = pay_channel **'groble'**(card 불요 — CHECK 선행 삭제) · 봇 생성분 **source='api' + memo 말미 `bot_approve:{승인자}`**(오너 직접분 manual/owner_sql 유지) · `BOT_PAY_AUTOCREATE` **기본 false → 9/3 컷오버 시 =true**(settlement-corrections **C-4** 등재) · 매핑표 금액 정가표 참조 승인 · **박지훈(76) 「승인됐는데 payments 없음」 조사 종결**(설계 인정). **등록계 쿨다운 시즌당 1회 구현**(같은 시즌 기결 신청 카운트 — 승인·반려 무관 · 차단 아님, 오너 DM+신청자 양쪽 경고 후 오너 재량) + pending SCD-2 해석(이전 계정 유지·전이는 승인 시점만) 승인 확정. **공개 카운트 API 스펙 발행**(site-redesign-v2 §3-1a — `GET /api/trainer-capacity`·집계 정본 v_panel_roster·cap 서버 정본·60s 캐시·degrade 폴백, 구현 미착수 유지). 🔴 **P-1 불일치 3차 회신 발신** — project_id 없음(어느 프로젝트에서도 미실행)·정기 점검 원시 출력 전문·재실측 2행/706,000 첨부, 「발행문 기대값(9행/3,164,000)이 실행 보고로 오독됨」 재확인. 동결(P-1 재실행·STEP D·후속 DDL) 유지 중. **사이트 v2 추가 — 실제 코칭 영상 섹션 설계 발행**(§3-3, 관제탑 8/22 추가 지시): 반별 3편(초→중→심) · lite-embed(썸네일 정적 + 클릭 시 iframe 주입 — 로드 시 iframe 0개로 자동재생 구조 차단) · **목록 데이터 기반 published 플래그(기본 false)** — 게이트 3항목(개인정보·동의·패치 의존) 반별 독립 판정, 통과분만 렌더·0건이면 섹션 미렌더·무리 확장 상세 앵커도 같은 상수라 미게시 반은 링크 미노출. 구현 미착수 유지. **🟢 P-1 동결 해제·위임 실행 완료(8/22)** — 관제탑이 오독 확정(불일치는 처음부터 없었음·3회 추궁 철회)하고 ⓐⓑⓒ 승인 후 위임. 사전검증(동명 1·연결 7행·기존재 0) → INSERT → **사후검증 3/3 통과: 9행·3,164,000 / kind=course 전체 11행 / 미연결 신종근·박성민 2행뿐**(ⓑ 승인 문구를 미연결 2행 memo에 반영). **⛔ STEP D는 드라이런에서 중단·회신** — 발행 블록과 실DB 3건 충돌: ① 양형석 course 4가 active·초급반이라 D-2가 8회 차감(방금 실행한 P-1 전환 음수 행과 정면 모순) ② 세션 8건 전부 '초급반' 라벨인데 8월 5건은 허혜민·김해주(심화반·8회 계약) 수업 개연성 ③ 6~7월 3건 참석자 불명(초급반 재적 권태완뿐). 사후검증(잔여 음수 0)이 이 상태를 통과시켜 검증으로 못 잡는 유형 — 세션×반×참석자 확정 요청. **FIFO 백필(D구간) 재산출·재발행(§7)** — 미귀속 seed외 62행/352판/20명 전원 다중 등록(B구간 0). 규칙: 트레이너 일치 필수(주현성 판례)·started_on FIFO·기귀속 선차감·경계 걸침 보류. 기존 23/125/10 폐기. **관제탑 8/22 판정·실행 완료**: ① P-1 **독립검증 합격 — 직강 결제 시드 종결**. ② FIFO **fit 41행 + straddle 3행((b) 선행 등록 초과 허용 채택) = 44행/260판 위임 실행 완료** — 사후검증 미귀속 62→**18**·귀속 **71**·음수 잔여 = 등록25 −4·33 −9·107 −6 정확히 3건(설계상 예상). ③ 검증 ③에서 **배치 밖 기존 귀속 2건 적발**: 주현성(60) 세션 117·120(8/16 준구 6판)이 현태 등록 102에 귀속 — 8/21 백필분, 신규칙(트레이너 일치)으론 102→128 재귀속 대상. 판정 대기·미실행. ④ **김예지 98번 확인 회신(실행 없음)**: DB의 −30은 98 하나뿐, memo가 「대상 세션 #93」 명시 — 같은 날 봇 4연타(93~96 각 10판) 중 3건 상쇄로 김예지 자신의 중복 정리로 산술 완결. 장익교·김재성엔 −30 부재(김재성 유일 음수 = 세션 90 −5). 봇기록 25행이 7/29·대상 #93이면 같은 건(시트 미러의 학생 칸 누락)이나 그 경우에도 장익교·김재성 −30은 별도 필요. ⑤ **STEP D 재판정 접수**: 앞선 3건 판정 폐기 — 세션 정본 = 수업_로그(506행), 양형석 course 4 **active 유지**(진행 7회·잔여 5·잔액 150,000 / 입금 620,000 vs 등록 360,000 차액 260,000 별도), DB이관 추출본(DB이관_세션·미기입_8월·반단위여부 열) 수령 후 D-1부터 재발행. ⑥ **STEP C 정정 2건(started_on 6행·C-2 7명)은 「직전 전달문」 미도달 — 원문 요청 회신.** **⑧ 재귀속 위임 2건 실행 완료(8/23)**: 주현성 117·120 → 등록128(사후검증 ①②④ 통과 · **③ 기대 27은 기실행 fit 139·141 10판 미합산 — 실측 소진 16·잔여 17로 중단·보고** · 부수 전수 조회: 트레이너/학생 불일치 잔존 0건) + 김예지 묶음 7행 → 등록101(별건 확정·보류 해제 — 미귀속 **11**(W-2만) · 등록101 −7/등록124 +7 = 학생 합 0 부담 설계 · 음수 4건 = 25·33·101·107). **⑦ /결제신청 신규 수강생 경로 설계 발행(8/23 · `payreq-new-student-flow.md`)** — payreq#9(김한성·45,000/10판) approved+student_id null 실측(전 큐 유일). 설계: 접수 자동완성(후보 `이름 #id·담당·상태` + 「➕ 신규 등록」 항목) · 신규는 승인 시 students INSERT 동반(승인 시점 exact 재검색으로 수동 등재 경합 방어·note 말미 `bot_payreq:#N`) · 동명 2+는 rank 자동선택 금지·오너 셀렉트 강제(정희준/정희훈·최재민 판례) · **student_id 미확정이면 approved 전이 차단**(신규는 INSERT 성공이 조건 — :2104 「null이어도 승인 유효」 폐기). 스키마 +1(`is_new_student`). BOT_PAY_AUTOCREATE 자동 생성 체인의 전제 조건. 김한성 = 신가 45,000 첫 적용 — 경과조치 4명 명단 불변 기록. 구현 금지 준수 — 봇 v2(8/24~27) 창구.
- **대기**: 관제탑 — **STEP C 정정 2건 원문 재전송**(started_on 6행 + C-2 7명 — 미도달) · 주현성 ③ 기대값 정정 확인(실측 16·17) · payreq 설계 검토(봇 v2 착수 승인) · autoplay=1 판정(v2 §3-3). 오너 — **DB이관 추출본**(DB이관_세션·미기입_8월 — STEP D 재발행 소스) · §20 DDL(등록계 게이트) · W-2(장익교·김재성 — 김예지 98 판정과도 연결) · W-5(신종근) · W-6(박성진·최창운) · 사례 6건 담당 지정 · 대표 후기 1줄 · 영상 게이트 3×3(v2 §3-3). 결제 트랙 — :396·:708 배포 + 정가표 상수 + payBase net 분리. 트레이너 — W-2(장익교·김재성) · W-3(준구 7/28~31). 결제 트랙 — :396·:708 배포 + 정가표 상수 + payBase net 분리.
- *(해소 8/17 — **§19f `payments.deposit_ref` DDL 실행 완료**(구 대기 ④). 오너 회신 없이 **실DB 직접 조회로 확인**: `information_schema.columns`에 `deposit_ref` 실재 · 부분 인덱스 `idx_payments_deposit_ref` 실재 · 사용 행 0 · `kind='set'` 행 0(실판매 전이라 정상). `supabase_admin_panel.sql` §19f의 「⚠️ 미실행」 표시를 실행 완료로 정정했다. `SCHEMA_OPTIONAL` 유지 — 실행은 됐지만 **읽는 코드가 아직 없어**(대조는 SQL 쿼리, 패널은 생성 차단만) REQUIRED 승격은 묶음을 읽기 시작하는 PR로 미룬다.)*
- *(⚠ 결제 트랙 확인 요청 2건 — **세트 판매가 급여·쓰기 경로에 닿는다.** ① **세트는 빵다 순매출 6% 대상에서 통째로 빠진다** — `computeStaffSalary`가 2026-08부터 `kind==='lesson'`만 base로 잡는데(`LESSON_ONLY_START`) 세트의 **레슨분**도 `kind='set'`이라 함께 걷어내진다. 실측: 560,000 세트 → 6% 제안 **0원**, 같은 금액이 `lesson`이면 **30,500원**. 강의·상담의 자동 확대를 막으려던 필터가 세트 레슨분까지 잡는 형태라 **의도 여부 판정 필요**(급여 산식이라 이 트랙은 코드 미변경). ② **`POST /api/admin/payments`에 `kind='set'` 차단 가드를 넣었다**(400 `set_needs_two_rows`) — 금액 산식은 손대지 않은 입력 형태 거부이고 `PANEL_WRITE` 미설정이라 현재 실동작 영향 0이지만, 결제 데이터 쓰기 경로라 통지한다. 이견이면 되돌리는 데 한 블록이다.)*
- *(해소 8/16 — **강의 축 SQL 5종 실행 완료**(구 대기 ③). 오너 회신 + 실DB 실측 일치: `courses.trainer_id`(bigint nullable) 실재 · `courses` **2행**(허혜민 id1 · 김해주 id2, 둘 다 `trainer_id=4`)으로 중복 없음(멱등 확인) · `payments` #137→course 1 / #142→course 2 귀속 · students 10·57 active · memo137 §9.4 문구 반영 · `notify pgrst` 완료. 따라서 staff-panel 오너 직강 섹션의 degrade가 풀린다.)*
- *(해소 8/16 — **`lesson_enrollments` 2컬럼 DDL + 백필 122행 실행 완료**. 오너는 이 건을 "유일한 실제 대기"로 봤으나 **실DB 조회 결과 이미 반영돼 있었다**: `paid_amount`(int nullable, 122/122 기입) · `bonus_games`(int not null default 0, 현재 전량 0) · `chk_le_paid_amount`·`chk_le_bonus_range` 정의가 오너 정본과 글자 단위로 일치. 정의 확인 2건도 모두 정합 — `games_total`은 보너스 **포함** 총 판수라 CHECK 방향이 맞고, 유상 판수 0(전액 보너스 등록)은 `refundAmount()`가 `paidGames > 0` 가드로 나눗셈 전에 차단한다(`admin-panel.js:198`). 남은 실제 작업은 §19b-1 **제약명 정정뿐**이었다.)*
- **다음**: 실행 순서(관제탑 확정) = ~~FIFO 즉시분~~(완료) → **STEP C started_on 정정 → C-2 7명**(원문 수령 후) → **DB이관 추출본 수령 → STEP D 재발행 → 위임 실행**. FIFO 잔여 보류 18행(W-2 11 + 김예지 7)은 판정 해소 후 2차. 미연결 2행(신종근·박성민)은 계약 행 확정 시 UPDATE 연결. 9/3 컷오버 준비(C-1~C-4). Groble Phase 1 구현은 오너 승인 후. 사이트 v2 구현은 9/4 이후 승인 대기. 미결 F(김현성 #109 담당)·G(7월분 3건 시트 대조). 봇 v2 재배선(8/24~27): 초과 진행 경고·[미수]/[부담] 버튼·가격 가드(정가표 상수 수령 후)·payment_requests.payment_id 링크 컬럼.
- *(⚠ 결제 트랙 확인 요청 — **정산 엔진 실측 2건, 둘 다 `gross` 클램프에 가려 화면에 안 보인다**(실함수 하네스 · 2026-08-17 스냅). ① **준구·현태 둘 다 `gross=0`이다.** `total`(컷오버 이후 미정산 발생분만)과 `paidOut`(역대 `payouts.gross` 전액)이 서로 다른 시간 축을 세고, 이월기준 2행(준구 1,606,400 · 현태 1,990,200)은 대가만 있고 발생이 없어 영구 마이너스 잔고를 만든다 — 준구 526,100−2,385,300 / 현태 619,000−3,732,600. 설계 의도였던 `settled_period` 도장은 **0건**(컷오버 이후 세션 72건). **이 상태에서는 전환 전후 비교를 `gross`로 못 한다 — `lesson_accrued`로 해야 한다.** ② **음수 정정 세션이 발생분에서 차감되지 않는다** — `admin-panel.js:241`의 `if (g <= 0) continue;`가 누산 전에 건너뛴다. 표시용 `newPlayed`는 음수를 포함하므로 **표시는 정정을 반영하고 발생분은 안 하는 비대칭**이다. 실측 2건 전부 준구 과다계상 — 세션 90(학생 8 · −5판 · 「오등록 정정」) ≈12,800 + 세션 98(학생 59 · −30판 · 「중복 등록 보정」) ≈80,000 = **≈92,800원**. 공교롭게 세션 기준 전환의 준구 +66,200과 방향이 반대라 부분 상쇄되지만 **무관한 오류**라 각각 고쳐야 한다. 상세·수정안 `docs/settlement-session-basis.md` §3·§8.1.)*
- *(백로그 — **환불 2건이 `kind='refund'`를 안 쓴다.** `payments_kind_check`에 `'refund'`가 있는데 실제 환불 #124·#126은 `kind='lesson'` + 음수 금액으로 들어가 있다. 컷오버 전 정리 대상이고 **지금 손대지 않는다**(오너 8/16 지시). 정산 엔진이 음수 행을 이미 정상 처리하므로 현행 산출값에는 영향이 없다 — 표기 일관성 문제다.)*
- *(등재 — **병행수강 운영 한계 2건, 뿌리가 같다**(스코프·기록 단위가 학생인데 계약이 사람보다 잘다). ① **준구 패널에 정희준 미표시** — 패널 스코프가 `students.trainer_id` 단일이라 담당=현태 행만 보인다. 준구에게 안내 완료. ② **준구가 정희준 수업을 `/수업등록`으로 기록 불가** — 봇에는 담당 검증이 없고(`resolveStudentId`는 rank 선호일 뿐 차단 아님) 실제 차단은 **시트가 `레슨로그_${trainer}` 탭 분리**라 notFound → DB 이중기록도 스킵된다. 임시 우회는 **시트 준구 탭에 명부 행만 추가**(Level 0 · `students` 행 추가 금지 — `/수강생등록` 경로는 중복 행을 만든다. 패널 대행은 `PANEL_WRITE` 미설정으로 423). 둘 다 **staff-panel·봇 v2의 등록(enrollment) 단위 전환(8/24~27)에서 해소** — 스코프·기록 해석을 학생에서 등록으로 내리면 병행수강이 예외 없이 표현된다.)*
- *(해소 8/14 — **운영 확인 3건**: PWA 설치 검증 4종 통과(오너 폰 실기 — 디코 인앱·PWA 아이콘 모두 로그인·조회 OK) · STEP 0 PART A-r 반영(중복 4행 삭제 실측 회신) · §18+`BOT_PAYREQ=1` 가동(/결제신청 등록 로그 실측). **B-1 위계**: 오너 확정 — #171(강의→세트→레슨→VIP) 유지, Manus 위계 기각, 재배열 없음. **B-2**: c안 확정 — 유료 레벨테스트 상담(20,000)은 명칭·가격 유지, 무료 LV.1 필기시험을 리드마그넷으로 연결만(신규 개발 금지). 아래 결제 트랙 확인 요청은 c안 확정으로 종결 — 가격 충돌 없음.)*
- *(⚠ 결제 트랙 확인 요청 — **「무료 LV.1 진단」은 게시 가격과 충돌한다.** `레벨테스트`는 `index.html` 17곳에 있고 실체가 유료 상담이다(`plan-note` "강의 상담(테스트) 비용 20,000원" · FAQ "레슨상담 15,000원 / 강의상담 20,000원" · CTA "레벨테스트 신청 →"이 유료 신청서로 연결). "무료"를 붙이면 **한 화면에 무료 ↔ 20,000원이 동시 표시**되므로 표기 정리가 아니라 **가격·표시광고 변경**이다. 실제 무료화(b안)를 택하면 상담비 정책 변경이라 결제 트랙 소관 — 명칭만 바꾸는 a안은 MRIacademy가 처리 가능. 오너 지정 대기.)*
- *(해소 8/14 — **7월 잠금 충돌 0건 실측 + 오너 동결 유지 확정**: 프로브에서 #35 games=21(54→21 정정 기완료)·15/44/21 결제 0건(병합 3건 기완료) — 판정 SQL이 잠금 전에 실행됐음이 확정. 해제·예외 등재 둘 다 불요(오너 판정 8/14).)*
- *(해소 8/14 — 잠금 트리거 null 커버리지: 오너 확인 완료 — **null 시 `paid_at` 월 폴백으로 잠금 검사, 이월(열린 달 지정) 경로 정상**. 결제 트랙 전달 완료. 잠금 구멍 없음 — 앞선 "#35 잠금 밖" 판독은 값 판독이었고 트리거 동작은 정의대로다.)*
- *(발견 8/14 — **허혜민 = 레슨·강의 병행 수강생.** [7] 실행 결과 신규가 아니라 기존 **id 57**(레슨·담당 현태·6/1 40,000 + 6/8 120,000 + 7/17 240,000) 실재 — 가드가 신규 행 생성을 막고 결제 #137(8/2 290,000·course)만 정상 삽입. 학생 행 담당은 레슨 담당(현태) 유지가 정상이고 강의 담당(무리)은 결제 memo에 있음 — 정희준 판례의 레슨/강의 버전. #137의 course 귀속은 P1 스테이징에서.)*
- *(해소 — 장익교 병합: 실행 후 실측 2행(5/07·7/15)·학생 1행(id9·담당 현태) 확인. §19 검증(a): `chk_payments_single_attribution` 실재 확인. 등록 시드: 무결성 쿼리 0·0·0. 유현 id77: DELETE 1. 김규완: 상담일 8/6·입금일 8/7 분리. 정희준: 동일인 병행수강(통장 실측).)*

## 메인(총괄·조정) — 2026-08-14

- **진행**: 이 문서 신설(#115) + `CLAUDE.md` 트랙 선언문을 **소관 정의표**로 재구성(#116) + 학습지 선언 블록 흡수(#117). gmi-clancup에도 갱신 규칙·제목 정정(#45). 이제 `CLAUDE.md`에 "이 세션의 소관" 문구 0건 — 소관은 정의표 한 곳에만 있다. 스킬 정리(#121 디자인 7개 삭제·한국어 트리거 / #127 복구 절차 문서 — 독점 라이선스라 본체 이관 불가). G드컵 시즌3 종료(#29·#31·#32·#33·#35·#102·#106). **DDL 기록이 8/4 "완료"(오기록) → 8/5 "미실행"(실측) → 8/5 "완료"(실제) 로 세 번 뒤집혔다. 원인은 규칙 공백 — 오너가 세션 밖에서 한 조치를 문서에 넣는 경로가 없었다. 상단 "오너 조치 반영"으로 보강.** 디자인 스킬 보강 4건 검토 완료 — 설치 3(`design-analysis`·`craft` 전역 / `web-design-guidelines` 저장소, MIT) · 보류 1(`designer-skills` 마켓플레이스는 스킬 96개라 후보 과다) · 참고자료 1(`awesome-claude-design` 클론만). 트리거 충돌 3건은 설명문 축소로 해소.
  **8/8~8/12 추가분**: 승인 자동화 3종(#170 — 허용목록·SQL 게이트·Code CI). Code CI는 `npm run check` + **HTML 인라인 `<script>` 문법 검사**로, 빌드가 없어 그대로 배포되던 사각지대를 막는다. 21st.dev MCP project scope(#170, `${API_KEY_21ST}` 참조·평문 없음 — 단 `21st.dev`가 이 컨테이너에서 네트워크 차단이라 미검증). **가격 개정 8/10 배포**(#171 — 레슨 45,000/90,000/140,000 · 세트 280,000/340,000/405,000 개편 + 위계 재편 강의→세트→레슨, VIP는 「프리미엄 1:1」로 하단 이동. FAQ에 가격표가 한 벌 더 있어 전수 스캔으로 잡았다). 히어로 KPI 중복 계산 제거(#171 — `counts.lessonseng+suganseng` 단순 덧셈 → `/api/enrollment`의 Set 중복제거 `count`). `docs/AUTH_DESIGN.md` 초안(#174).
  **8/13 추가분**: 오너 통합 지시를 트랙 분류 — A(레슨 용어·추천대상·FAQ)·B(Manus 흡수)·C1(패널 메인 재편)·동명가드 → MRIacademy / C2(직원 급여 제외)·apply.html 결제 안내 문구 → 결제 / D(데이터 대조) → 메인 처리 완료. staff-panel PWA 매니페스트도 MRIacademy로 전달. **단 PWA·C1은 이미 #180으로 구현·배포 완료라 전달 불요**(근거: `panel.webmanifest` 존재·`scope:"/staff-panel.html"`, `staff-panel.html:5-7`의 manifest·theme-color·apple 메타, `.ptabs`/`.tabpane` 탭 구조와 195행 "G드컵·정산·급여·도구는 탭 뒤 기본 숨김" 주석. `site.webmanifest`·`index.html` 무수정 확인). 사이트 개편 A·B는 진행 중, `apply.html` 결제 안내 문구는 확정안(🏦 계좌 안내) 발사됨 — **남은 전달분은 직원 1명(`staff` id=1) 급여 제외 1건뿐.** **외부 감사 3건 중 apply.html 건은 감사의 원인 진단이 틀렸다** — `PAYMENT_LIVE=false`라 토스 위젯은 애초에 미노출이고, 실제 원인은 신청 버튼 하단 `apply.html:213`의 "💳 …결제가 진행됩니다" 문구다(추가 아닌 교체가 맞다). 열린 이슈 6건이 전부 `track:casino`라 분류만 하고 상태 변경은 오너 승인 후 처리(#122 종료 · #123·#124·#125·#120 시즌4 백로그 · #130 유지). **#123·#124·#120은 코드 미수정 부채라 종료 금지.** 외부 제안 채택 2건은 `docs/copy-compliance-audit.md`로 조사만 완료(수정 미착수·승인 대기) — 최상위 발견은 **① `index.html`의 "트레이너 자동 배정"이 미구현**(배정 코드 부재, 챗봇 시스템 프롬프트 `server.js:550`이 오히려 "배정하지 말라"고 지시해 정면 충돌) **② 전적 수집 동의(`statsConsent`)를 프론트가 보내는데 서버가 한 번도 읽지 않아 동의 기록이 남지 않는다**(문구 문제가 아니라 구현 누락) **③ "사이트에 저장되지 않습니다"가 실제 보관처(디스코드 웹훅·Google 시트 영구 보관)와 불일치** **④ 성별이 클라이언트·서버 양쪽 필수인데 사용처가 임베드 표시뿐.** **8/14 오너 승인 — 1~6 착수(전부 MRIacademy 소관, 지시문 전달), 7(`privacy.html` 위탁 목록)은 보류**(결제 트랙 토스 심사 완료 후 위탁 관계가 확정되면 그때 문안을 잡는다 — 심사 전에 고치면 두 번 고치게 된다). **감사 이후 범위가 줄었다**: A2·A4(#192)가 퍼널 `s-desc`를 3단 프로세스로 교체하면서 "결제 → AI 진단 → 자동 배정" 한 줄이 이미 사라져, 자동배정 주장은 `index.html:826`·`861` 2곳만 남았다.
- **대기**: 오너 — ① 결제·학습지·카지노 트랙 `CLAUDE.md`에 갱신 규칙 반영(해당 저장소 미접근 — 저장소명 주시면 `add_repo`로 처리) ② 각 트랙 세션에 "`CLAUDE.md`에 자기 소관 블록을 추가하지 말 것, 정의표 수정은 메인에 요청" 공지 ③ 스킬 트리거 실측(파일 첨부 필요) ④ `designer-skills` 96개 중 골라 쓸지 여부 ⑤ **브랜치 107개 삭제 스크립트 실행** — 이 세션에서 `git push --delete`가 HTTP 403으로 거부된다(토큰 권한 또는 Ruleset). 로컬 실행 필요 ⑥ `work/ai-setup` 미흡수 2건 처리 방침 ⑦ `student-progress`·`gmi-progress` 공개 범위 판단 ⑧ Supabase Auth 도입 시점(설계서는 Phase 1 현행 JWT 유지를 권고) ⑨ ~~`/결제신청` 실사용 1건~~ → **해소**(오너 8/14 조치. 근거: 이 문서 MRIacademy 섹션 「§18+`BOT_PAYREQ=1` 가동(/결제신청 등록 로그 실측)」. 8/13 조사 당시 `payment_requests` 0행이었던 건 미발송 때문이었고 버그가 아니었다) ⑩ ~~직원 1명(`staff` id=1) 급여 제외 건 결제 트랙 전달~~ → **해소**(결제 트랙 대기 ⑤에 「C2 직원 급여 제외(`staff` id=1)」로 이미 등재돼 있다 — 이 문서 결제 섹션 2026-08-13. 전달이 도달했다는 뜻이므로 메인의 전달 과제는 닫는다. **단 실행은 결제 트랙 대기로 남아 있고, `admin-panel.js:315`가 `active`를 거르지 않으므로 DB update 단독으로는 목록에서 사라지지 않는다** — 코드 1줄이 함께 가야 한다).
- **다음**: 트랙 간 충돌·공용 스키마 조정. 소관 불명 작업의 1차 분류. **오너 회신의 빈 칸을 완료로 옮기지 않는다** — 값이 없으면 대기로 남긴다.
- *(정정 — `AUTH_DESIGN.md` §6이 "v1.1 문서를 저장소에서 못 찾았다"로 나갔는데, 직후 MRIacademy 트랙의 STATE 갱신으로 골자가 들어왔다(D-day 9/3 · PR-1~6 · 승인 큐는 PR-3a). §6.1을 정정했다. 근거: 이 문서 MRIacademy 섹션 2026-08-11.)*
- *(기록 — `work/ai-setup`(미머지) 대조 결과 14개 규칙 중 12개는 `CLAUDE.md`에 흡수됐고 **2개가 공백**이다: ① env 실제 값 읽기·출력 금지 ② 디스코드 대량 발송·고객 대상 메시지 승인. 부수로 `SECURITY.md`가 GitHub 기본 템플릿 그대로이고, `CLAUDE.md:89`가 존재하지 않는 `AGENTS.md`를 참조한다.)*
- *(확정 — **수강생 등록 규칙 v2**(오너 8/13, v1 대체). **등록 시점 = 수강 확정**(레슨비·직강비 입금). **상담 단계 이력은 사이트 상담신청 폼이 자동으로 담당하고 오너 수기 등록은 없다.** 예외 둘: ① **트레이너 상담 건은 트레이너 본인이 `/수강생등록`** — 등록해야 수당이 귀속되므로 인센티브가 정렬돼 있다(`admin-panel.js:302-308`이 `students` 행의 `trainer_id`를 거쳐야만 건당 1만을 붙인다. 무료 상담은 `amount > 0` 가드로 이미 제외) ② **오너 직접 상담·강의 상담은 등록 불요, 전환 시 등록.** 기시드된 리드 3명(id 78·79·80)은 유지하고 **상담 후 2주 미전환 시 `status='done'`**(전원 오너 직접 상담이라 수당 귀속 이슈 없음 — "담당 배정 필수" 항목은 해제). `status='done'`은 DDL 불요이고 `idx_students_trainer`가 `status <> 'done'` 부분 인덱스라 설계가 이 용법을 전제한다.)*
- *(기록 — **`payments.student_id`가 `not null references students(id)`**(`supabase_admin_panel.sql:41`)다. 즉 **`students` 행이 없으면 그 사람의 결제는 DB에 기록 자체가 불가능**하다. 규칙 v2의 "오너 직접 상담은 등록 불요"와 맞물리면 **오너 직접 상담의 상담비 매출이 DB에서 누락**된다. 지금은 시트가 정산 정본이라 실害가 없고, 빵다 순매출 6%도 8월부터 `kind='lesson'` 한정이라 영향이 없다. 다만 **컷오버(9/3)로 정본이 DB로 넘어가면 그대로 굳는다.** → **확정(오너 8/14) — ①안 채택: `students` 행을 등록하고 상담 후 2주 미전환 시 `status='done'`.** 상담 리드 3명(id 78·79·80)이 이미 이 패턴으로 운영 중이라 신설이 아니라 기존 운영의 명문화다. **②`student_id` nullable 완화는 기각**(귀속·정산 리스크 — 결제가 학생에 안 붙으면 `computeStudent`와 상담 수당 귀속이 함께 흔들린다), **③시트 전용도 기각**(컷오버 원칙 위반). **즉 등록 규칙 v2의 "오너 직접 상담은 등록 불요"는 상담비를 받은 건에는 적용되지 않는다 — 돈이 들어오면 행을 만든다.**)*
- *(설계 메모 — **가격·카피·시즌 상태의 단일 정본화**. 오너 8/13 우선순위 상향. 지금은 정본이 HTML·`server.js`·시트에 흩어져 있어 어긋남 사고가 반복됐다(구 가중치표 노출 · FAQ 가격표 중복 · 규칙 변경 시 안내문 미수정 3건). 방향은 스냅샷 JSON 안 A — 빌드 단계가 없는 정적 배포라 **서버가 `GET /api/site-meta`로 정본을 내리고 프론트는 렌더만** 한다(`GDCUP_WEIGHT_S3` → `/api/gdcup-meta` 패턴의 확장. 프론트 하드코딩 금지가 이미 이 저장소 규칙이다). **착수는 컷오버(9/3) 이후** — 그 전에 손대면 시트↔DB 병행 중인 가격 정본이 셋으로 늘어난다. 회귀 테스트 최소 세트(신청·할인·차감·정산·환불)도 같은 시점에 이슈로 개설한다. 정본이 하나가 된 뒤라야 "무엇을 기준으로 통과인지"가 정의된다.)*
- *(백로그 등재만 — **상황 선택 퍼널 · 분석 이벤트 스펙**(오너 8/13, 외부 제안). **착수 금지, 컷오버(9/3) 이후.** 두 건 모두 "무엇을 성공으로 볼지"가 먼저 정해져야 하는데 그 정본이 아직 시트↔DB로 갈려 있다. 현재 계측은 `umami.track("apply_submit")` 2회 호출(`apply.html:347`·`349`)이 전부이고 **같은 제출에 두 번 찍혀 이벤트 수가 부풀 여지가 있다** — 스펙을 짤 때 이 중복부터 정리해야 한다. 퍼널 단계 정의는 `docs/copy-compliance-audit.md` 2-A의 실제 흐름(신청 → 운영진 수동 배정)을 기준으로 잡을 것. 사이트 문구의 "결제 → 진단 → 자동 배정"은 실제 퍼널이 아니다.)*
- *(종결 8/14 — **「무료 LV.1 진단」 트랙 간 판단 요청**(MRIacademy가 "오너 지정 대기"로 올린 건). 메인이 `index.html` 18곳을 실측해 **유료 상담과의 가격 충돌이 실재함**을 확인하고 3안(a′ 명칭에서 "무료" 제거 / b 실제 무료화=결제 트랙 / c 별도 상품)으로 정리해 a′을 권고했다. **오너는 c안으로 확정**했다 — 유료 레벨테스트 상담(20,000)은 명칭·가격 그대로 두고, **무료 LV.1 필기시험을 별도 리드마그넷으로 연결만** 한다(신규 개발 금지). c안이 a′보다 낫다: 개명이 아예 없어 `apply.html` `consultFee()`의 `applyType` 값 동결 문제도 함께 비껴간다. **조정 종결 — 메인 후속 없음.**)*

## 카지노 — 2026-08-11

- **진행**: **카지노 오픈 무기한 보류**(오너 8/11 — 아카데미 매출 작업이 전사 1순위). 유지보수 모드 = 신규 기능 중단·배포 동결. 시즌3은 **오프라인 확정으로 종결**(DB 입력 안 함)하고 최종 순위를 명예의 전당에 정적 박제 — #70 머지(1 현태팀 59 / 2 싱싱미역 48 / 3 록초리단 38 / 4 영앤리치레코즈 35 / 킬MVP hand3manone 15킬. 로스터는 공개 IGN만, `results.html`엔 표를 복제하지 않음, `시즌3 신청` 문구도 같은 PR에서 아카이브 표현으로 정리). ⚠ **`GDCUP_PAGES`가 서빙하는 이 저장소 사본(`gdcup-history.html`)도 같이 맞췄다**(#172) — gmi-clancup만 고치면 `/gdcup-history`에는 안 나온다. #165와 같은 함정을 그대로 밟았고, 머지 직후 두 사본을 대조해서 발견했다. **gdcup 페이지를 고칠 때는 항상 사본 두 벌을 함께 본다.** ⚠ **카지노 백엔드는 이미 가동 중이다** — `CASINO_ENABLED=1`(크래시 라운드 실제 순환: 8/10 하루 `R#30460`→`R#32142`), 운영시간 **18:00~03:00 KST**가 양쪽 경계에서 실측 확인됨(09:05 UTC 라운드 진행 / 17:59 UTC 정지). 즉 "오픈"은 코인 경제 시작이 아니라 **웹 UI 공개**이고, 실잔액이 이미 쌓여 있어 초기화·리디노미네이션 재실행은 파괴적이다. 주간 캡(적립·베팅 각 1,800)은 env 미설정 → 코드 기본값이 정본이고, ledger 합계 파생이라 **월 00:00 KST 자동 롤오버(리셋 작업 자체가 불필요)**. SQLite는 Railway 볼륨 `/data` 마운트 확인.
- **대기**: 오너 — ① **`gdcup_scores` 시즌3 잔재 1행 삭제**(`id=49` R1 록초리단, SQL은 #70 본문. 데이터 삭제 = Level 0. 미실행 시 results·overlay에 반쪽 순위표 노출) ② **`gmi-clancup`(public)에 실계좌·실명이 커밋돼 있다** — `cogs/_utils.py:134`·`env.example:17`. 해당 파이썬은 운영 봇이 아닌 **죽은 사본**(실물은 `gmi-casino-bot`)이라 삭제해도 기능 영향 0. 착수 여부·히스토리 purge 판단 필요 ③ 오픈 재개 시 **`CRASH_MAX_BET`·`DUEL_STAKE_CAP` env 실값 확인**(env가 코드를 이긴다. 리디노미네이션 전 값이 남아 있으면 최소 베팅 100 > 최대 5로 크래시가 통째로 잠긴다. 값 조회는 세션에서 차단됨) ④ **싱싱미역 상금 이체 — 주신 IGN 4개 중 `dk____dk`·`tkfkdgodyPRF` 2개가 시즌3 전체에 없다**(교체 이력도 없음. DB 확정 로스터는 `qormwhwakdrpa18`·`dkdkPRF`·`9sin--`·`GOBAEPIL`) ⑤ 유진(SaNaee_KR) 스팀 계정 육안 확인 ⑥ 계좌 미등록분 — 입상팀 우선.
- **다음**: **대기 모드.** 재개 시 ③ env 검증 → 오픈 안내 문구(`_casino_gate.py:65`가 아직 "8월 9일 오픈 예정") 수정 → `/카지노중단` 즉시 킬스위치 신설(현재는 Railway에서 `CASINO_ENABLED=0`, 재배포 탓에 1~2분 소요) → Railway startCommand `main.py` 불일치 정리(파일이 없는데 대시보드가 그걸 가리킴. `railway.json`이 이겨서 동작 중이나 그 파일이 사라지면 즉사) → #130 GmI 이전 설계 조사 · 시즌 목록 하드코딩 제거 · `GDCUP_ADMIN_KEY` 교체(다다님 위임분) · Pages 복구 시 미러·라우트 정리 · overlay/roster 디자인 정합 · `MaxListenersExceededWarning`(interactionCreate 11개) · `dist refreshed total=0` 원인 · #120·#122·#124.
- *(해소 — 싱싱미역 정산 계좌: `gdcup_payouts` 시즌3 4행 전원 실명·은행·계좌·예금주가 채워져 있고 예금주=실명 일치. 값은 열람하지 않고 `is not null` 판정만 했다. 교체 이력 없음(`roster_log` 빈 배열, 예비 0명).)*
- *(해소 — 본선 당일 항목: `match-pull` R1 전수 확인 · 현태팀 대승 인게임 닉 · 옵저버 CSV 인코딩 실기 · 디스코드 공지 채널명. 시즌3이 종료돼 지난 항목이라 내렸다. `apply.sql`도 시즌3 점수가 오프라인 확정으로 종결되면서 실효성이 사라졌다 — **실행됐다는 뜻이 아니라 더 이상 결과에 영향이 없다는 뜻**이다.)*

<!-- 시즌3 본선(2026-08-07) 기록 — 보류 해제 시 참고 -->
- *(본선 아카이브: 확정 16팀, 20:00 솔로 → 21:00 팀전 5R. 오늘 배포분 전부 Railway 반영 완료: **T0 팀장 +1 폐지**(#164 산정식·meta / #68 정본 룰북 / #165 Railway 서빙 사본 — 늘크크만 40→39·×0.85→×0.92, 시드 6↔7 싱싱미역 교환) · **live-board 16팀 잘림 수정**(#167·#69) · **match-pull 킬 미귀속 색출**(#168 — 응답에 `missing[]`·`missingUnexplained`, 교체는 `roster_log` 계획과 실측이 **둘 다** 맞을 때만 정상 처리해 오탐 차단. 읽기전용·DDL 없음). 19:26 재기동 로그 `[schema] OK` 전 테이블 정상, 본선 트래픽 5xx 0건. 티어는 **오너 수동 판정이 최종**(#136). 앞선 준비분은 라이브 킬 트래커(#151·#61) · 시청자 토토(#147) · 운영진 매뉴얼(#155·#62) · 정산 CSV 빈 줄 출력(#146) · 점수입력 닉 드롭다운(#148·#60), 16/16 BPI 검산 일치. ⚠ **Pages 배포가 8/5 이후 전부 실패 → 본선 정본은 Railway 주소**(#138·#140), 그리고 **`GDCUP_PAGES` 화이트리스트가 이 저장소 사본을 서빙**하므로 룰북을 gmi-clancup만 고치면 화면에 안 나온다(#165가 그 사례 — Pages 복구 시 사본 정리 대상).
- *(위 아카이브의 미해결 항목은 전부 현재 **대기**·**다음**으로 옮겼다. 남은 항목은 지났거나 시즌3 종료로 실효성이 사라진 것들이다.)*
