# 트랙별 현황 (세션 간 공유)

> 세션 시작 시 이 문서를 먼저 읽는다. 작업 종료 시 **자기 트랙 섹션만** 갱신한다.
> 형식은 3줄(진행/대기/다음) 고정. 길게 쓰지 않는다 — 상세는 PR 본문과 `docs/`에 남긴다.
> 날짜는 KST. 제목의 날짜는 그 섹션을 마지막으로 갱신한 날이다.
>
> **오너 조치 반영 (2026-08-05 추가).** DDL 실행·env 설정·시트 수정·입금처럼 **오너가 세션 밖에서
> 하는 일**은 어느 트랙의 "작업 종료"도 아니라서 갱신 트리거가 걸리지 않는다. 그 사실을 들은 세션이
> **타 트랙 섹션이라도 그 대기 항목을 해소 처리한다** — 이 경우만 "자기 섹션만" 규칙의 예외다.
> 대신 **무엇을 근거로 고쳤는지**(확인 쿼리·로그 등)를 한 줄로 함께 남긴다.
> 근거 없이 오너 말만으로 "완료"로 바꾸지 않는다.

---

## 결제 — 2026-08-05

- **진행**: 강의 결제를 `payments`에 편입할지 검토 중(편입 시 빵다 순매출 6%와 트레이너 상담 가산이 자동 증가 — Level 0). 레슨 가격 조정 8/10 적용 대기(10판 45,000 / 21판 90,000 / 33판 140,000, 반영 위치 7곳 확인 완료·미착수).
- **대기**: 오너 — ① `T2_CRON` / `DIRECT_STATUS` env 상태 회신(꺼져 있으면 크론 전체 미작동) ② 강의 결제 `payments` 편입 여부 ③ 가격 PR 준비 시점(지금 / 8-9).
- **다음**: env 확인 → 편입 여부 확정(수강관리 P1의 선행 조건) → 8/10 가격 PR 머지.

## 학습지 — 2026-08-05

- **진행**: 이 세션에서 파악된 내용 없음.
- **대기**: —
- **다음**: 해당 트랙이 첫 갱신 시 채울 것.

## MRIacademy(수강관리) — 2026-08-05

- **진행**: 오너 판정 회신 접수·시트 반영 목록 전달(중복 6건·박성민 정정·이월메모 갱신, 현재값 가드 포함). 가불 금지를 설계문서 §4.5로 문서화(P3 "재결제 안내" 요구사항 포함). **✅ §16(student_aliases)·§17(courses 3종)·payments.course_id DDL 실행 완료** — 확인 쿼리 4행 정상(course_attendance·course_sessions·courses·student_aliases). 8/4~8/5의 "미실행" 기록은 해소됐다. **import 2종·정정 SQL은 아직 미적용.**
- **대기**: 오너 — ① import 실행(전달본 재실행 or 오늘 판정 통합 재생성 — 재생성 권고) ② 조현준 중복 판정 ③ 차감 누락 14건 세션별 확인 ④ 박성민 30,000 입금(기록은 결제 트랙) ⑤ 가불 금지 시행 시점·기존 초과자 예외.
- **다음**: import 실행 → 검산(김준성 17회·453,344 / 박성민 13회·초과1) → `courses.verified_at` → 본인 연결 승인 큐 → `my.html` + `/api/me` → P2 봇 커맨드.
- *(이 섹션은 2026-08-05 오너 지시로 메인 트랙이 대신 갱신했다 — DDL 실행 사실이 이 트랙 세션에 전달되지 않아 어긋나 있었다. 근거: 오너 확인 쿼리 4행.)*

## 메인(총괄·조정) — 2026-08-06

- **진행**: 이 문서 신설(#115) + `CLAUDE.md` 트랙 선언문을 **소관 정의표**로 재구성(#116) + 학습지 선언 블록 흡수(#117). gmi-clancup에도 갱신 규칙·제목 정정(#45). 이제 `CLAUDE.md`에 "이 세션의 소관" 문구 0건 — 소관은 정의표 한 곳에만 있다. 스킬 정리(#121 디자인 7개 삭제·한국어 트리거 / #127 복구 절차 문서 — 독점 라이선스라 본체 이관 불가). G드컵 시즌3 종료(#29·#31·#32·#33·#35·#102·#106). **DDL 기록이 8/4 "완료"(오기록) → 8/5 "미실행"(실측) → 8/5 "완료"(실제) 로 세 번 뒤집혔다. 원인은 규칙 공백 — 오너가 세션 밖에서 한 조치를 문서에 넣는 경로가 없었다. 상단 "오너 조치 반영"으로 보강.** 디자인 스킬 보강 4건 검토 완료 — 설치 3(`design-analysis`·`craft` 전역 / `web-design-guidelines` 저장소, MIT) · 보류 1(`designer-skills` 마켓플레이스는 스킬 96개라 후보 과다) · 참고자료 1(`awesome-claude-design` 클론만). 트리거 충돌 3건은 설명문 축소로 해소.
- **대기**: 오너 — ① 결제·학습지·카지노 트랙 `CLAUDE.md`에 갱신 규칙 반영(해당 저장소 미접근 — 저장소명 주시면 `add_repo`로 처리) ② 각 트랙 세션에 "`CLAUDE.md`에 자기 소관 블록을 추가하지 말 것, 정의표 수정은 메인에 요청" 공지 ③ 옵저버 CSV 실기 테스트 ④ `match-pull` 리허설(응답 `season: 3`) ⑤ 스킬 트리거 실측(파일 첨부 필요) ⑥ `designer-skills` 96개 중 골라 쓸지 여부.
- **다음**: 트랙 간 충돌·공용 스키마 조정. 소관 불명 작업의 1차 분류. **오너 회신의 빈 칸을 완료로 옮기지 않는다** — 값이 없으면 대기로 남긴다.

## 카지노 — 2026-08-07

- **진행**: **본선 당일 · 13팀 확정**(C조 취소 / YM·록초리단·현태팀 추가). 티어는 **오너 수동 판정이 최종**(#136 — confirm·edit·reverify 3경로의 자동 덮어쓰기 차단, `GDCUP_AUTO_TIER=1`로 복귀 가능). 13팀 BPI·가중치를 저장 티어에서 재계산해 **13/13 일치** 확인(T0 팀장 +1·S 누진 포함). **⚠ GitHub Pages 배포가 8/5 이후 전부 실패**(Actions 인프라, 재실행도 queued 고착) → **본선 정본은 Railway 주소**(#138·#140, 16페이지 화이트리스트 서빙). 오늘 신규 — **라이브 킬 트래커**(#151/gmi-clancup#61, `/live`·`/live-board`: 전멸 순서로 순위점까지 역산, `gdcup_scores` 무접촉) · **시청자 토토**(#147, `/toto`, 마감 23:15 KST) · **운영진 매뉴얼**(#155/#62, `/staff-guide`) · 정산 CSV가 계좌 미등록 멤버도 빈 줄로 출력(#146, 3인팀 상금 분할 버그 동반 수정) · 점수입력 선수닉 드롭다운(#148/#60). `gdcup_toto`·`gdcup_live` **DDL 실행·검증 완료**(컬럼·유니크 인덱스·RLS·anon 권한, 재기동 자기점검 로그 `[schema] OK`). 순위 계산을 `gdcupComputeStandings` 순수 함수로 공용화 — 라이브 보드가 식을 복제하지 않는다(등가성 43케이스 응답 전문 비교). **본선 최종 16팀 확정**(BZNAXIS·DAWN·dawnfan 추가, 대기 신청 0건) — 가칭 전면 개명(A조→아아 한잔주세요/AHJ · B조→영앤리치레코즈/YNR · D조→형진업고튀어/HJT · BZNAXIS→Dawn얼굴/DWF · dawnfan→Dawnfan/DFN), 로스터 정정 4팀, **16/16 검산 일치**. 신규 3색은 ΔE·다크배경 대비로 검증(16색 최소 ΔE 24.2, 전 색 4.5:1 이상 — 제안받은 진보라 `#7C3AED`는 A조색과 ΔE 14.4로 충돌해 교체). **T0 팀장 +1 폐지**(#164 산정식·meta, #68 정본 룰북, **#165 Railway 서빙 사본**) — 늘크크만 영향 40→39·×0.85→×0.92, 시드 6↔7 싱싱미역 교환. ⚠ **`GDCUP_PAGES` 화이트리스트가 이 저장소의 사본을 서빙**하므로 룰북 수정은 gmi-clancup만 고치면 화면에 안 나온다(#165가 그 사례 — Pages 복구 시 사본 정리 대상).
- **대기**: 오너 — ① **`apply.sql` 실행 + 검증 SELECT 2건 회신**(로스터·개명·늘크크 저장값 반영. 미실행 시 늘크크 저장 40 ↔ 배포 코드 39 어긋난 채 유지) ② 유진(SaNaee_KR) **스팀 계정 접속 육안 확인**(카배 접속 시 킬 귀속 실패, `roster_log` 상시 플래그) ③ 디스코드 공지 채널명(staff-guide §06 미정 1건) ④ 현태팀 대승 인게임 닉(미수신 시 CSV 공란 유지) ⑤ 옵저버 CSV 인코딩(리허설 첫 순서로 실기 후 확정. 4종 파일 전달 완료) ⑥ 리허설 시각 다다님 확정 ⑦ **계좌 미등록 26/52명** — 본선 후 입상팀부터 수집 ⑧ match-pull 리허설.
- **다음**: 16:00 리허설(옵저버 CSV 실기) → 20:00 솔로(집계 분리·시스템 미기록) → **21:00 팀전 5라운드 지원** → 본선 후: 라운드별·선수별 킬 CSV(아카이브·차기 BPI 참고) · Pages 복구 시 미러·라우트 정리 · `GDCUP_ADMIN_KEY` 교체(다다님 위임분) · #130 GmI 이전 설계 조사 · 시즌 목록 하드코딩 제거 · overlay/roster 디자인 정합 · `MaxListenersExceededWarning`(interactionCreate 11개) · `dist refreshed total=0` 원인 · #120·#122·#124.
- *(해소 — C조: DB `status=cancelled` 실측. `GDCUP_APPLY_DEADLINE`: Railway 변수 목록에 존재 확인, 서버 재기동 후이므로 반영됨. team_brand: 13팀 전원 태그·색 등록 DB 실측(현태팀은 딱퍼와 노랑 충돌로 `#FFFFFF` 교체). 맵: 룰북에 로테이션 표 대신 한 줄 표기로 확정.)*
