# 등록계 미등록자 검수·DM — 설계 (2026-08-25 · 관제탑 지시)

> GmI 트랙 소관 · 카지노 휴면 중 관제탑 승인 경유 대행분. **설계 발행까지 — 구현은
> §20+pws_eligible DDL 실행·43시즌 공지 이후, 관제탑 착수 승인 후.**
> 43시즌 재등록 드라이브와 묶어 진행한다(8/25 밤 시즌 정정 — 현재 42가 정확한 값이라
> 밀림 자체가 없었다. 재등록으로 season=43 행이 쌓이며 정상 진행).

## 0. 실측 전제 (2026-08-25)

| 항목 | 실측 |
|---|---|
| `clan_registry` | **38명 · 전량 season=42** (등록 07-25~07-30, 이후 신규 0) |
| 클랜 인원 | 약 80명(관제탑) → **미등록 약 42명** |
| `GatewayIntentBits.GuildMembers` | **이미 켜져 있다**(`server.js:850`) — 멤버 목록 fetch 가능 |
| `clan_registry.discord_id` | 존재(등록 키) — 대조 축 성립 |
| 봇 cron 인프라 | `maybeRunDaily` 패턴 기동 중 — 필요 시 정기 검수 편입 여지 |

## 1. 흐름 4단계 (지시 1:1)

### 1-1. 미등록자 목록 산출 — 디스코드 멤버 ↔ `clan_registry.discord_id`

```
guild.members.fetch()  (LESSON_GUILD_ID = GmI 서버)
  → bot 계정 제외
  → clan_registry에서 discord_id 전량 조회(시즌 무관 — 한 번이라도 등록한 사람은 제외)
  → 차집합 = 미등록자
```

- **시즌 무관 대조가 맞다**: 43시즌 재등록 대상은 "42시즌 등록자"와 "완전 미등록자"가
  다르다 — 전자는 재등록 안내, 후자는 신규 등록 안내로 **DM 문안이 갈린다**(§1-3).
  산출은 두 집합을 분리해 낸다: `미등록(0회)` / `구시즌만(42) 등록`.
- 대량 fetch는 1회성 명령 안에서만(상시 캐시 금지 — 멤버 목록은 개인정보 축).

### 1-2. 등급 역할별 분류 (G · m · I) — 상위 등급 우선

- 역할 ID는 **기존 env 3개 재사용**: `DISCORD_ROLE_G` · `DISCORD_ROLE_M` · `DISCORD_ROLE_I`
  (8/25 실측 정정 — `/등록계현황` 미등록 섹션이 이미 이 이름을 읽는다(server.js:2222).
  구 표기 `GMI_ROLE_*`로 신설하면 두 기능이 서로 다른 env를 보게 된다.)
  (역할 이름 문자열 매칭은 개명에 깨진다 — ID 고정).
- 멤버의 보유 역할 중 최상위 하나로 분류. 어느 역할도 없으면 `기타`.
- 출력 정렬: **G → m → I → 기타** (상위 등급 미등록자 우선 대상 — 지시 그대로).

### 1-3. DM 발송 — 문안은 관제탑 발행

- **트리거는 오너 전용 명령** `/등록계검수` (옵션 `dm발송: true/false`):
  - `false`(기본) = 목록만 오너 DM으로(드라이런) — **발송 전 반드시 이 목록을 본다.**
  - `true` = 확인 버튼(오너) 후 발송.
- 문안 2종(관제탑 발행 대기): ① 완전 미등록자용(신규 등록 안내) ② 42시즌 등록자용
  (43시즌 재등록 안내 — 전환 마감 9/2·본인 명의 원칙 포함).
- **페이싱 필수**: 건당 1.2s 간격(디스코드 DM rate limit — 대량 DM은 차단 사유).
  40여 건 ≈ 1분. 진행/실패를 오너에게 요약 회신.
- **DM 차단 계정은 실패 목록으로 회신**(강제 불가) — 채널 멘션 공지로 보완(운영 판단).
- 발송 이력: `ops_state`에 `regaudit:<날짜>` 키로 기록(재실행 시 중복 발송 방지 —
  같은 날 재실행이면 미발송분만).

### 1-4. `/등록계현황`에 「미등록 N명」 섹션

- 기존 현황(오너 DM 전용)에 **수치 요약** 추가: `미등록 N명 (G a · m b · I c · 기타 d)`.
- **이름 목록은 현황 본문에 넣지 않는다** — 길이 폭발(디스코드 2,000자)과 노출 최소화.
  목록이 필요하면 `/등록계검수`(드라이런)로 뽑는다.
- 멤버 fetch 실패(권한·장애) 시 섹션 생략 — 현황의 다른 섹션을 죽이지 않는다(기존
  degrade 관례).

## 2. 구현 규모 (착수 승인 후)

| 항목 | 규모 |
|---|---|
| `/등록계검수` 명령 + 대조·분류·드라이런 | ~60줄 |
| DM 발송(페이싱·확인 버튼·실패 집계) | ~40줄 |
| `/등록계현황` 미등록 섹션 | ~10줄 |
| env 3개(`DISCORD_ROLE_G/M/I`) | ✅ **Railway 등록 확인(관제탑 실측 8/25 — 값은 조회 불가)**. 「미설정 추정」은 철회. 실기 `/등록계현황`에서 「조회 생략」이 뜰 때만 값을 채운다 |
| DDL | **없음** (기존 테이블 읽기만) |

## 3. 게이트·순서 (8/25 밤 현행화)

1. ~~§20 + `clan_registry` 확장 DDL~~ — ✅ **완료(8/25 밤 · 오너 지시 위임 실행 —
   `season43-cutover.md` §0-(2) 검증 지문)**
2. **43시즌 공지 발행 — 오너/관제탑 · 유일한 남은 선행**
3. ~~관제탑 문안 2종 + 착수 승인~~ — ✅ **완료(8/25 밤 · §4 등재, 착수 승인 수령 —
   「단 구현은 공지 이후 순서 유지」)**
4. 구현·배포 → 드라이런 목록 오너 확인 → DM 발송

## 4. DM 문안 2종 (관제탑 발행 2026-08-25 · 원문 등재 — 발송 시 이 문안 그대로)

### 4-1. A안 — 완전 미등록자용

```
OO님 안녕하세요, 무리입니다 👋

43시즌이 9월 9일부터 시작돼요.
등급 심사가 등록계 기준으로 진행되는데
아직 등록이 안 되어 있으셔서 안내드립니다.

봇 DM에서 `/등록계` 한 번만 실행해 주시면 됩니다. 1분이면 끝나요.

■ 등록해두시면
· 등급 심사 대상이 됩니다
· 매일 갱신되는 GmI 순위표에 이름이 올라갑니다
· PWS 출전 자격 확인도 여기서 이뤄집니다

■ 본인 명의 계정으로 부탁드려요
가족 명의 계정은 대회 규정상 인정이 안 돼서요.
헷갈리시면 편하게 물어봐 주세요 🙏
```

### 4-2. B안 — 42시즌 등록자 → 43시즌 갱신용

```
OO님 안녕하세요, 무리입니다 😊

42시즌 등록계 등록해 주셔서 감사합니다.
9월 9일부터 43시즌이 시작돼서 갱신 안내드려요.

봇 DM에서 `/등록계` 한 번만 다시 실행해 주시면 됩니다.
계정이 그대로시면 확인만 하고 넘어가시면 돼요.

■ 계정을 바꾸셨다면
수정하시면 저한테 승인 요청이 자동으로 갑니다.
승인 전까지는 이전 계정이 심사 기준으로 남아요.

■ 전환 신청 마감 — 9월 2일 (화)
이후 신청은 44시즌에 반영됩니다.

잠깐만 시간 내주시면 감사하겠습니다 🙏
```
